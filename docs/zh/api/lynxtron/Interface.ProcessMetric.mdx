{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}
import \* as Lynx from '@lynx';

[lynxtron](/api/lynxtron/index.mdx) / ProcessMetric

# 接口: ProcessMetric

## 属性

### cpu

```ts
cpu: CPUUsage;
```

CPU usage of the process.

#### 定义于

lynxtron/apis/api/process-metric.d.ts:45

---

### creationTime

```ts
creationTime: number;
```

Creation time for this process. The time is represented as number of
milliseconds since epoch. Since the `pid` can be reused after a process dies, it
is useful to use both the `pid` and the `creationTime` to uniquely identify a
process.

#### 定义于

lynxtron/apis/api/process-metric.d.ts:52

---

### integrityLevel?

```ts
optional integrityLevel:
  | "untrusted"
  | "low"
  | "medium"
  | "high"
  | "unknown";
```

One of the following values:

#### Platform

win32

#### 定义于

lynxtron/apis/api/process-metric.d.ts:58

---

### memory

```ts
memory: MemoryInfo;
```

Memory information for the process.

#### 定义于

lynxtron/apis/api/process-metric.d.ts:62

---

### name?

```ts
optional name: string;
```

The name of the process. Examples for utility: `Audio Service`, `Content
Decryption Module Service`, `Network Service`, `Video Capture`, etc.

#### 定义于

lynxtron/apis/api/process-metric.d.ts:67

---

### pid

```ts
pid: number;
```

Process id of the process.

#### 定义于

lynxtron/apis/api/process-metric.d.ts:71

---

### sandboxed?

```ts
optional sandboxed: boolean;
```

Whether the process is sandboxed on OS level.

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/process-metric.d.ts:77

---

### serviceName?

```ts
optional serviceName: string;
```

The non-localized name of the process.

#### 定义于

lynxtron/apis/api/process-metric.d.ts:81

---

### type

```ts
type:
  | "Browser"
  | "Tab"
  | "Utility"
  | "Zygote"
  | "Sandbox helper"
  | "GPU"
  | "Pepper Plugin"
  | "Pepper Plugin Broker"
  | "Unknown";
```

Process type. One of the following values:

#### 定义于

lynxtron/apis/api/process-metric.d.ts:85
