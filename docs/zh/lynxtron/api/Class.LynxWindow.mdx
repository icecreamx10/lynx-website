{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}
import \* as Lynx from '@lynx';

[lynxtron](/lynxtron/api/index.mdx) / LynxWindow

# 类: LynxWindow

## 构造函数

### new LynxWindow()

```ts
new LynxWindow(options?: LynxWindowConstructorOptions): LynxWindow
```

LynxWindow

#### 参数

| 范围       | 类型                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------ |
| `options`? | [`LynxWindowConstructorOptions`](/lynxtron/api/Interface.LynxWindowConstructorOptions.mdx) |

#### 返回

[`LynxWindow`](/lynxtron/api/Class.LynxWindow.mdx)

#### 定义于

lynxtron/apis/api/lynx-window.d.ts:277

## 方法

### loadFile()

```ts
loadFile(path: string): boolean
```

Load template data for the window.

#### 参数

| 范围   | 类型     |
| ------ | -------- |
| `path` | `string` |

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/lynx-window.d.ts:294

---

### sendGlobalEvent()

```ts
sendGlobalEvent(eventName: string, data: Object): boolean
```

Send global event to the window.

#### 参数

| 范围        | 类型     |
| ----------- | -------- |
| `eventName` | `string` |
| `data`      | `Object` |

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/lynx-window.d.ts:302

---

### updateData()

```ts
updateData(data: Object, globalProps: Object): boolean
```

Update template data for the window.

#### 参数

| 范围          | 类型     |
| ------------- | -------- |
| `data`        | `Object` |
| `globalProps` | `Object` |

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/lynx-window.d.ts:298

---

### fromId()

```ts
static fromId(id: number): null | LynxWindow
```

The window with the given `id`.

#### 参数

| 范围 | 类型     |
| ---- | -------- |
| `id` | `number` |

#### 返回

`null` \| [`LynxWindow`](/lynxtron/api/Class.LynxWindow.mdx)

#### 定义于

lynxtron/apis/api/lynx-window.d.ts:282

---

### getAllWindows()

```ts
static getAllWindows(): LynxWindow[]
```

An array of all opened browser windows.

#### 返回

[`LynxWindow`](/lynxtron/api/Class.LynxWindow.mdx)[]

#### 定义于

lynxtron/apis/api/lynx-window.d.ts:286

---

### getFocusedWindow()

```ts
static getFocusedWindow(): null | LynxWindow
```

The window that is focused in this application, otherwise returns `null`.

#### 返回

`null` \| [`LynxWindow`](/lynxtron/api/Class.LynxWindow.mdx)

#### 定义于

lynxtron/apis/api/lynx-window.d.ts:290
