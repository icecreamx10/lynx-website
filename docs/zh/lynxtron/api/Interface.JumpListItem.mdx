{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}
import \* as Lynx from '@lynx';

[lynxtron](/lynxtron/api/index.mdx) / JumpListItem

# 接口: JumpListItem

## 属性

### args?

```ts
optional args: string;
```

The command line arguments when `program` is executed. Should only be set if
`type` is `task`.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:42

---

### description?

```ts
optional description: string;
```

Description of the task (displayed in a tooltip). Should only be set if `type`
is `task`. Maximum length 260 characters.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:47

---

### iconIndex?

```ts
optional iconIndex: number;
```

The index of the icon in the resource file. If a resource file contains multiple
icons this value can be used to specify the zero-based index of the icon that
should be displayed for this task. If a resource file contains only one icon,
this property should be set to zero.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:54

---

### iconPath?

```ts
optional iconPath: string;
```

The absolute path to an icon to be displayed in a Jump List, which can be an
arbitrary resource file that contains an icon (e.g. `.ico`, `.exe`, `.dll`). You
can usually specify `process.execPath` to show the program icon.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:60

---

### path?

```ts
optional path: string;
```

Path of the file to open, should only be set if `type` is `file`.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:64

---

### program?

```ts
optional program: string;
```

Path of the program to execute, usually you should specify `process.execPath`
which opens the current program. Should only be set if `type` is `task`.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:69

---

### title?

```ts
optional title: string;
```

The text to be displayed for the item in the Jump List. Should only be set if
`type` is `task`.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:74

---

### type?

```ts
optional type: "task" | "separator" | "file";
```

One of the following:

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:78

---

### workingDirectory?

```ts
optional workingDirectory: string;
```

The working directory. Default is empty.

#### 定义于

lynxtron/apis/api/jump-list-item.d.ts:82
