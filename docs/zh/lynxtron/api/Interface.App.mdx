{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

[lynxtron](/lynxtron/api/index.mdx) / App

# 接口: App

Control your application's event lifecycle.

## 继承

- `EventEmitter`

## 属性

### applicationMenu

```ts
applicationMenu: null | Menu;
```

A `Menu | null` property that returns `Menu` if one has been set and `null`
otherwise. Users can pass a Menu to set this property.

#### 定义于

lynxtron/apis/api/app.d.ts:1729

---

### badgeCount

```ts
badgeCount: number;
```

An `Integer` property that returns the badge count for current app. Setting the
count to `0` will hide the badge.

On macOS, setting this with any nonzero integer shows on the dock icon. On
Linux, this property only works for Unity launcher.

> [!NOTE] Unity launcher requires a `.desktop` file to work. For more
> information, please read the Unity integration documentation.

> [!NOTE] On macOS, you need to ensure that your application has the permission
> to display notifications for this property to take effect.

#### Platform

linux,darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1745

---

### commandLine

```ts
readonly commandLine: CommandLine;
```

A `CommandLine` object that allows you to read and manipulate the command line
arguments that Chromium uses.

#### 定义于

lynxtron/apis/api/app.d.ts:1751

---

### dock

```ts
readonly dock: undefined | Dock;
```

A `Dock | undefined` property (`Dock` on macOS, `undefined` on all other
platforms) that allows you to perform actions on your app icon in the user's
dock.

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1759

---

### isPackaged

```ts
readonly isPackaged: boolean;
```

A `boolean` property that returns `true` if the app is packaged, `false`
otherwise. For many apps, this property can be used to distinguish development
and production environments.

#### 定义于

lynxtron/apis/api/app.d.ts:1766

---

### name

```ts
name: string;
```

A `string` property that indicates the current application's name, which is the
name in the application's `package.json` file.

Usually the `name` field of `package.json` is a short lowercase name, according
to the npm modules spec. You should usually also specify a `productName` field,
which is your application's full capitalized name, and which will be preferred
over `name` by Electron.

#### 定义于

lynxtron/apis/api/app.d.ts:1776

---

### runningUnderARM64Translation

```ts
readonly runningUnderARM64Translation: boolean;
```

A `boolean` which when `true` indicates that the app is currently running under
an ARM64 translator (like the macOS Rosetta Translator Environment or Windows
WOW).

You can use this property to prompt users to download the arm64 version of your
application when they are mistakenly running the x64 version under Rosetta or
WOW.

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1788

## 方法

### \[captureRejectionSymbol\]()?

```ts
optional [captureRejectionSymbol]<K>(
   error: Error,
   event: string | symbol, ...
   args: AnyRest): void
```

#### 类型参数

| 类型参数 |
| -------- |
| `K`      |

#### 参数

| 范围      | 类型                 |
| --------- | -------------------- |
| `error`   | `Error`              |
| `event`   | `string` \| `symbol` |
| ...`args` | `AnyRest`            |

#### 返回

`void`

#### 继承自

`EventEmitter.[captureRejectionSymbol]`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:131

---

### addListener()

#### addListener(event, listener)

```ts
addListener(event: "activate", listener: (event: Event, hasVisibleWindows: boolean) => void): this
```

##### 参数

| 范围       | 类型                                                         |
| ---------- | ------------------------------------------------------------ |
| `event`    | `"activate"`                                                 |
| `listener` | (`event`: `Event`, `hasVisibleWindows`: `boolean`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:314

#### addListener(event, listener)

```ts
addListener(event: "activity-was-continued", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"activity-was-continued"`                                            |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:382

#### addListener(event, listener)

```ts
addListener(event: "before-quit", listener: (event: Event) => void): this
```

Alias for `emitter.on(eventName, listener)`.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"before-quit"`              |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:428

#### addListener(event, listener)

```ts
addListener(event: "continue-activity", listener: (event: Event, type: string, userInfo: unknown, details: ContinueActivityDetails) => void): this
```

##### 参数

| 范围       | 类型                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `event`    | `"continue-activity"`                                                                                                                                              |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`, `details`: [`ContinueActivityDetails`](/lynxtron/api/Interface.ContinueActivityDetails.mdx)) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:497

#### addListener(event, listener)

```ts
addListener(event: "continue-activity-error", listener: (event: Event, type: string, error: string) => void): this
```

##### 参数

| 范围       | 类型                                                              |
| ---------- | ----------------------------------------------------------------- |
| `event`    | `"continue-activity-error"`                                       |
| `listener` | (`event`: `Event`, `type`: `string`, `error`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:587

#### addListener(event, listener)

```ts
addListener(event: "did-become-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-become-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:638

#### addListener(event, listener)

```ts
addListener(event: "did-resign-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-resign-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:668

#### addListener(event, listener)

```ts
addListener(event: "new-window-for-tab", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"new-window-for-tab"`       |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:698

#### addListener(event, listener)

```ts
addListener(event: "open-file", listener: (event: Event, path: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"open-file"`                                  |
| `listener` | (`event`: `Event`, `path`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:739

#### addListener(event, listener)

```ts
addListener(event: "open-url", listener: (event: Event, url: string) => void): this
```

##### 参数

| 范围       | 类型                                          |
| ---------- | --------------------------------------------- |
| `event`    | `"open-url"`                                  |
| `listener` | (`event`: `Event`, `url`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:774

#### addListener(event, listener)

```ts
addListener(event: "quit", listener: (event: Event, exitCode: number) => void): this
```

Alias for `emitter.on(eventName, listener)`.

##### 参数

| 范围       | 类型                                               |
| ---------- | -------------------------------------------------- |
| `event`    | `"quit"`                                           |
| `listener` | (`event`: `Event`, `exitCode`: `number`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:794

#### addListener(event, listener)

```ts
addListener(event: "ready", listener: (event: Event, launchInfo: Record<string, any> | NotificationResponse) => void): this
```

Alias for `emitter.on(eventName, listener)`.

##### 参数

| 范围       | 类型                                                                                                                                                  |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `event`    | `"ready"`                                                                                                                                             |
| `listener` | (`event`: `Event`, `launchInfo`: `Record`\<`string`, `any`\> \| [`NotificationResponse`](/lynxtron/api/Interface.NotificationResponse.mdx)) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:845

#### addListener(event, listener)

```ts
addListener(event: "second-instance", listener: (event: Event, argv: string[], workingDirectory: string, additionalData: unknown) => void): this
```

Alias for `emitter.on(eventName, listener)`.

##### 参数

| 范围       | 类型                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------- |
| `event`    | `"second-instance"`                                                                                         |
| `listener` | (`event`: `Event`, `argv`: `string`[], `workingDirectory`: `string`, `additionalData`: `unknown`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:942

#### addListener(event, listener)

```ts
addListener(event: "update-activity-state", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"update-activity-state"`                                             |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1039

#### addListener(event, listener)

```ts
addListener(event: "will-continue-activity", listener: (event: Event, type: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"will-continue-activity"`                     |
| `listener` | (`event`: `Event`, `type`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1117

#### addListener(event, listener)

```ts
addListener(event: "will-finish-launching", listener: () => void): this
```

Alias for `emitter.on(eventName, listener)`.

##### 参数

| 范围       | 类型                      |
| ---------- | ------------------------- |
| `event`    | `"will-finish-launching"` |
| `listener` | () => `void`              |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1151

#### addListener(event, listener)

```ts
addListener(event: "will-quit", listener: (event: Event) => void): this
```

Alias for `emitter.on(eventName, listener)`.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"will-quit"`                |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1167

#### addListener(event, listener)

```ts
addListener(event: "window-all-closed", listener: () => void): this
```

Alias for `emitter.on(eventName, listener)`.

##### 参数

| 范围       | 类型                  |
| ---------- | --------------------- |
| `event`    | `"window-all-closed"` |
| `listener` | () => `void`          |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.addListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1182

---

### addRecentDocument()

```ts
addRecentDocument(path: string): void
```

Adds `path` to the recent documents list.

This list is managed by the OS. On Windows, you can visit the list from the task
bar, and on macOS, you can visit it from dock menu.

#### 参数

| 范围   | 类型     |
| ------ | -------- |
| `path` | `string` |

#### 返回

`void`

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1192

---

### clearRecentDocuments()

```ts
clearRecentDocuments(): void
```

Clears the recent documents list.

#### 返回

`void`

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1198

---

### emit()

```ts
emit<K>(eventName: string | symbol, ...args: AnyRest): boolean
```

Synchronously calls each of the listeners registered for the event named`eventName`, in the order they were registered, passing the supplied arguments
to each.

Returns `true` if the event had listeners, `false` otherwise.

```js
import EventEmitter from 'node:events';
const myEmitter = new EventEmitter();

// First listener
myEmitter.on('event', function firstListener() {
  console.log('Helloooo! first listener');
});
// Second listener
myEmitter.on('event', function secondListener(arg1, arg2) {
  console.log(`event with parameters ${arg1}, ${arg2} in second listener`);
});
// Third listener
myEmitter.on('event', function thirdListener(...args) {
  const parameters = args.join(', ');
  console.log(`event with parameters ${parameters} in third listener`);
});

console.log(myEmitter.listeners('event'));

myEmitter.emit('event', 1, 2, 3, 4, 5);

// Prints:
// [
//   [Function: firstListener],
//   [Function: secondListener],
//   [Function: thirdListener]
// ]
// Helloooo! first listener
// event with parameters 1, 2 in second listener
// event with parameters 1, 2, 3, 4, 5 in third listener
```

#### 类型参数

| 类型参数 |
| -------- |
| `K`      |

#### 参数

| 范围        | 类型                 |
| ----------- | -------------------- |
| `eventName` | `string` \| `symbol` |
| ...`args`   | `AnyRest`            |

#### 返回

`boolean`

#### 添加于

v0.1.26

#### 继承自

`EventEmitter.emit`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:747

---

### eventNames()

```ts
eventNames(): (string | symbol)[]
```

Returns an array listing the events for which the emitter has registered
listeners. The values in the array are strings or `Symbol`s.

```js
import EventEmitter from 'node:events';
const myEE = new EventEmitter();
myEE.on('foo', () => {});
myEE.on('bar', () => {});

const sym = Symbol('symbol');
myEE.on(sym, () => {});

console.log(myEE.eventNames());
// Prints: [ 'foo', 'bar', Symbol(symbol) ]
```

#### 返回

(`string` \| `symbol`)[]

#### 添加于

v6.0.0

#### 继承自

`EventEmitter.eventNames`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:810

---

### exit()

```ts
exit(exitCode?: number): void
```

Exits immediately with `exitCode`. `exitCode` defaults to 0.

All windows will be closed immediately without asking the user, and the
`before-quit` and `will-quit` events will not be emitted.

#### 参数

| 范围        | 类型     |
| ----------- | -------- |
| `exitCode`? | `number` |

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1205

---

### focus()

```ts
focus(options?: any): void
```

On Linux, focuses on the first visible window. On macOS, makes the application
the active app. On Windows, focuses on the application's first window.

You should seek to use the `steal` option as sparingly as possible.

#### 参数

| 范围       | 类型  |
| ---------- | ----- |
| `options`? | `any` |

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1212

---

### getApplicationInfoForProtocol()

```ts
getApplicationInfoForProtocol(url: string): Promise<ApplicationInfoForProtocolReturnValue>
```

Resolve with an object containing the following:

- `icon` NativeImage - the display icon of the app handling the protocol.
- `path` string - installation path of the app handling the protocol.
- `name` string - display name of the app handling the protocol.

This method returns a promise that contains the application name, icon and path
of the default handler for the protocol (aka URI scheme) of a URL.

#### 参数

| 范围  | 类型     |
| ----- | -------- |
| `url` | `string` |

#### 返回

`Promise`\<[`ApplicationInfoForProtocolReturnValue`](/lynxtron/api/Interface.ApplicationInfoForProtocolReturnValue.mdx)\>

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1225

---

### getApplicationNameForProtocol()

```ts
getApplicationNameForProtocol(url: string): string
```

Name of the application handling the protocol, or an empty string if there is no
handler. For instance, if Electron is the default handler of the URL, this could
be `Electron` on Windows and Mac. However, don't rely on the precise format
which is not guaranteed to remain unchanged. Expect a different format on Linux,
possibly with a `.desktop` suffix.

This method returns the application name of the default handler for the protocol
(aka URI scheme) of a URL.

#### 参数

| 范围  | 类型     |
| ----- | -------- |
| `url` | `string` |

#### 返回

`string`

#### 定义于

lynxtron/apis/api/app.d.ts:1238

---

### getAppMetrics()

```ts
getAppMetrics(): ProcessMetric[]
```

Array of `ProcessMetric` objects that correspond to memory and CPU usage
statistics of all the processes associated with the app.

#### 返回

[`ProcessMetric`](/lynxtron/api/Interface.ProcessMetric.mdx)[]

#### 定义于

lynxtron/apis/api/app.d.ts:1243

---

### getAppPath()

```ts
getAppPath(): string
```

The current application directory.

#### 返回

`string`

#### 定义于

lynxtron/apis/api/app.d.ts:1247

---

### getBadgeCount()

```ts
getBadgeCount(): number
```

The current value displayed in the counter badge.

#### 返回

`number`

#### Platform

linux,darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1253

---

### getCurrentActivityType()

```ts
getCurrentActivityType(): string
```

The type of the currently running activity.

#### 返回

`string`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1259

---

### getJumpListSettings()

```ts
getJumpListSettings(): JumpListSettings
```

- `minItems` Integer - The minimum number of items that will be shown in the
  Jump List (for a more detailed description of this value see the MSDN docs).
- `removedItems` JumpListItem[] - Array of `JumpListItem` objects that
  correspond to items that the user has explicitly removed from custom categories
  in the Jump List. These items must not be re-added to the Jump List in the
  **next** call to `app.setJumpList()`, Windows will not display any custom
  category that contains any of the removed items.

#### 返回

[`JumpListSettings`](/lynxtron/api/Interface.JumpListSettings.mdx)

#### Platform

win32

#### 定义于

lynxtron/apis/api/app.d.ts:1271

---

### getLocaleCountryCode()

```ts
getLocaleCountryCode(): string
```

User operating system's locale two-letter ISO 3166 country code. The value is
taken from native OS APIs.

> [!NOTE] When unable to detect locale country code, it returns empty string.

#### 返回

`string`

#### 定义于

lynxtron/apis/api/app.d.ts:1278

---

### getLoginItemSettings()

```ts
getLoginItemSettings(options?: LoginItemSettingsOptions): LoginItemSettings
```

If you provided `path` and `args` options to `app.setLoginItemSettings`, then
you need to pass the same arguments here for `openAtLogin` to be set correctly.

- `openAtLogin` boolean - `true` if the app is set to open at login.
- `openAsHidden` boolean _macOS_ _Deprecated_ - `true` if the app is set to open
  as hidden at login. This does not work on macOS 13 and up.
- `wasOpenedAtLogin` boolean _macOS_ - `true` if the app was opened at login
  automatically.
- `wasOpenedAsHidden` boolean _macOS_ _Deprecated_ - `true` if the app was
  opened as a hidden login item. This indicates that the app should not open any
  windows at startup. This setting is not available on MAS builds or on macOS 13
  and up.
- `restoreState` boolean _macOS_ _Deprecated_ - `true` if the app was opened as
  a login item that should restore the state from the previous session. This
  indicates that the app should restore the windows that were open the last time
  the app was closed. This setting is not available on MAS builds or on macOS 13
  and up.
- `status` string _macOS_ - can be one of `not-registered`, `enabled`,
  `requires-approval`, or `not-found`.
- `executableWillLaunchAtLogin` boolean _Windows_ - `true` if app is set to open
  at login and its run key is not deactivated. This differs from `openAtLogin` as
  it ignores the `args` option, this property will be true if the given executable
  would be launched at login with **any** arguments.
- `launchItems` Object[] _Windows_
  - `name` string _Windows_ - name value of a registry entry.
  - `path` string _Windows_ - The executable to an app that corresponds to a
    registry entry.
  - `args` string[] _Windows_ - the command-line arguments to pass to the
    executable.
  - `scope` string _Windows_ - one of `user` or `machine`. Indicates whether the
    registry entry is under `HKEY_CURRENT USER` or `HKEY_LOCAL_MACHINE`.
  - `enabled` boolean _Windows_ - `true` if the app registry key is startup
    approved and therefore shows as `enabled` in Task Manager and Windows settings.

#### 参数

| 范围       | 类型                                                                               |
| ---------- | ---------------------------------------------------------------------------------- |
| `options`? | [`LoginItemSettingsOptions`](/lynxtron/api/Interface.LoginItemSettingsOptions.mdx) |

#### 返回

[`LoginItemSettings`](/lynxtron/api/Interface.LoginItemSettings.mdx)

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1317

---

### getMaxListeners()

```ts
getMaxListeners(): number
```

Returns the current max listener value for the `EventEmitter` which is either
set by `emitter.setMaxListeners(n)` or defaults to defaultMaxListeners.

#### 返回

`number`

#### 添加于

v1.0.0

#### 继承自

`EventEmitter.getMaxListeners`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:663

---

### getName()

```ts
getName(): string
```

The current application's name, which is the name in the application's
`package.json` file.

Usually the `name` field of `package.json` is a short lowercase name, according
to the npm modules spec. You should usually also specify a `productName` field,
which is your application's full capitalized name, and which will be preferred
over `name` by Electron.

#### 返回

`string`

#### 定义于

lynxtron/apis/api/app.d.ts:1327

---

### getPath()

```ts
getPath(name:
  | "home"
  | "appData"
  | "assets"
  | "userData"
  | "sessionData"
  | "temp"
  | "exe"
  | "module"
  | "desktop"
  | "documents"
  | "downloads"
  | "music"
  | "pictures"
  | "videos"
  | "recent"
  | "logs"
  | "crashDumps"): string
```

A path to a special directory or file associated with `name`. On failure, an
`Error` is thrown.

If `app.getPath('logs')` is called without called `app.setAppLogsPath()` being
called first, a default log directory will be created equivalent to calling
`app.setAppLogsPath()` without a `path` parameter.

#### 参数

| 范围   | 类型                                                                                                                                                                                                                                                     |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `name` | \| `"home"` \| `"appData"` \| `"assets"` \| `"userData"` \| `"sessionData"` \| `"temp"` \| `"exe"` \| `"module"` \| `"desktop"` \| `"documents"` \| `"downloads"` \| `"music"` \| `"pictures"` \| `"videos"` \| `"recent"` \| `"logs"` \| `"crashDumps"` |

#### 返回

`string`

#### 定义于

lynxtron/apis/api/app.d.ts:1336

---

### getRecentDocuments()

```ts
getRecentDocuments(): string[]
```

An array containing documents in the most recent documents list.

#### 返回

`string`[]

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1361

---

### getVersion()

```ts
getVersion(): string
```

The version of the loaded application. If no version is found in the
application's `package.json` file, the version of the current bundle or
executable is returned.

#### 返回

`string`

#### 定义于

lynxtron/apis/api/app.d.ts:1367

---

### hasSingleInstanceLock()

```ts
hasSingleInstanceLock(): boolean
```

This method returns whether or not this instance of your app is currently
holding the single instance lock. You can request the lock with
`app.requestSingleInstanceLock()` and release with
`app.releaseSingleInstanceLock()`

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/app.d.ts:1374

---

### hide()

```ts
hide(): void
```

Hides all application windows without minimizing them.

#### 返回

`void`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1380

---

### isDefaultProtocolClient()

```ts
isDefaultProtocolClient(
   protocol: string,
   path?: string,
   args?: string[]): boolean
```

Whether the current executable is the default handler for a protocol (aka URI
scheme).

> [!NOTE] On macOS, you can use this method to check if the app has been
> registered as the default protocol handler for a protocol. You can also verify
> this by checking `~/Library/Preferences/com.apple.LaunchServices.plist` on the
> macOS machine. Please refer to Apple's documentation for details.

The API uses the Windows Registry and `LSCopyDefaultHandlerForURLScheme`
internally.

#### 参数

| 范围       | 类型       |
| ---------- | ---------- |
| `protocol` | `string`   |
| `path`?    | `string`   |
| `args`?    | `string`[] |

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/app.d.ts:1393

---

### isEmojiPanelSupported()

```ts
isEmojiPanelSupported(): boolean
```

whether or not the current OS version allows for native emoji pickers.

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/app.d.ts:1401

---

### isHidden()

```ts
isHidden(): boolean
```

`true` if the application—including all of its windows—is hidden (e.g. with
`Command-H`), `false` otherwise.

#### 返回

`boolean`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1408

---

### isInApplicationsFolder()

```ts
isInApplicationsFolder(): boolean
```

Whether the application is currently running from the systems Application
folder. Use in combination with `app.moveToApplicationsFolder()`

#### 返回

`boolean`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1415

---

### isReady()

```ts
isReady(): boolean
```

`true` if Electron has finished initializing, `false` otherwise. See also
`app.whenReady()`.

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/app.d.ts:1420

---

### listenerCount()

```ts
listenerCount<K>(eventName: string | symbol, listener?: Function): number
```

Returns the number of listeners listening to the event named `eventName`.

If `listener` is provided, it will return how many times the listener
is found in the list of the listeners of the event.

#### 类型参数

| 类型参数 |
| -------- |
| `K`      |

#### 参数

| 范围        | 类型                 | 描述                                     |
| ----------- | -------------------- | ---------------------------------------- |
| `eventName` | `string` \| `symbol` | The name of the event being listened for |
| `listener`? | `Function`           | The event handler function               |

#### 返回

`number`

#### 添加于

v3.2.0

#### 继承自

`EventEmitter.listenerCount`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:757

---

### listeners()

```ts
listeners<K>(eventName: string | symbol): Function[]
```

Returns a copy of the array of listeners for the event named `eventName`.

```js
server.on('connection', (stream) => {
  console.log('someone connected!');
});
console.log(util.inspect(server.listeners('connection')));
// Prints: [ [Function] ]
```

#### 类型参数

| 类型参数 |
| -------- |
| `K`      |

#### 参数

| 范围        | 类型                 |
| ----------- | -------------------- |
| `eventName` | `string` \| `symbol` |

#### 返回

`Function`[]

#### 添加于

v0.1.26

#### 继承自

`EventEmitter.listeners`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:676

---

### moveToApplicationsFolder()

```ts
moveToApplicationsFolder(options?: MoveToApplicationsFolderOptions): boolean
```

Whether the move was successful. Please note that if the move is successful,
your application will quit and relaunch.

No confirmation dialog will be presented by default. If you wish to allow the
user to confirm the operation, you may do so using the `dialog` API.

**NOTE:** This method throws errors if anything other than the user causes the
move to fail. For instance if the user cancels the authorization dialog, this
method returns false. If we fail to perform the copy, then this method will
throw an error. The message in the error should be informative and tell you
exactly what went wrong.

By default, if an app of the same name as the one being moved exists in the
Applications directory and is _not_ running, the existing app will be trashed
and the active app moved into its place. If it _is_ running, the preexisting
running app will assume focus and the previously active app will quit itself.
This behavior can be changed by providing the optional conflict handler, where
the boolean returned by the handler determines whether or not the move conflict
is resolved with default behavior. i.e. returning `false` will ensure no
further action is taken, returning `true` will result in the default behavior
and the method continuing.

For example:

Would mean that if an app already exists in the user directory, if the user
chooses to 'Continue Move' then the function would continue with its default
behavior and the existing app will be trashed and the active app moved into its
place.

#### 参数

| 范围       | 类型                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------------ |
| `options`? | [`MoveToApplicationsFolderOptions`](/lynxtron/api/Interface.MoveToApplicationsFolderOptions.mdx) |

#### 返回

`boolean`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1453

---

### off()

#### off(event, listener)

```ts
off(event: "activate", listener: (event: Event, hasVisibleWindows: boolean) => void): this
```

##### 参数

| 范围       | 类型                                                         |
| ---------- | ------------------------------------------------------------ |
| `event`    | `"activate"`                                                 |
| `listener` | (`event`: `Event`, `hasVisibleWindows`: `boolean`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:300

#### off(event, listener)

```ts
off(event: "activity-was-continued", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"activity-was-continued"`                                            |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:348

#### off(event, listener)

```ts
off(event: "before-quit", listener: (event: Event) => void): this
```

Alias for `emitter.removeListener()`.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"before-quit"`              |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 添加于

v10.0.0

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:426

#### off(event, listener)

```ts
off(event: "continue-activity", listener: (event: Event, type: string, userInfo: unknown, details: ContinueActivityDetails) => void): this
```

##### 参数

| 范围       | 类型                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `event`    | `"continue-activity"`                                                                                                                                              |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`, `details`: [`ContinueActivityDetails`](/lynxtron/api/Interface.ContinueActivityDetails.mdx)) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:461

#### off(event, listener)

```ts
off(event: "continue-activity-error", listener: (event: Event, type: string, error: string) => void): this
```

##### 参数

| 范围       | 类型                                                              |
| ---------- | ----------------------------------------------------------------- |
| `event`    | `"continue-activity-error"`                                       |
| `listener` | (`event`: `Event`, `type`: `string`, `error`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:553

#### off(event, listener)

```ts
off(event: "did-become-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-become-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:630

#### off(event, listener)

```ts
off(event: "did-resign-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-resign-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:660

#### off(event, listener)

```ts
off(event: "new-window-for-tab", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"new-window-for-tab"`       |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:690

#### off(event, listener)

```ts
off(event: "open-file", listener: (event: Event, path: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"open-file"`                                  |
| `listener` | (`event`: `Event`, `path`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:728

#### off(event, listener)

```ts
off(event: "open-url", listener: (event: Event, url: string) => void): this
```

##### 参数

| 范围       | 类型                                          |
| ---------- | --------------------------------------------- |
| `event`    | `"open-url"`                                  |
| `listener` | (`event`: `Event`, `url`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:766

#### off(event, listener)

```ts
off(event: "quit", listener: (event: Event, exitCode: number) => void): this
```

Alias for `emitter.removeListener()`.

##### 参数

| 范围       | 类型                                               |
| ---------- | -------------------------------------------------- |
| `event`    | `"quit"`                                           |
| `listener` | (`event`: `Event`, `exitCode`: `number`) => `void` |

##### 返回

`this`

##### 添加于

v10.0.0

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:792

#### off(event, listener)

```ts
off(event: "ready", listener: (event: Event, launchInfo: Record<string, any> | NotificationResponse) => void): this
```

Alias for `emitter.removeListener()`.

##### 参数

| 范围       | 类型                                                                                                                                                  |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `event`    | `"ready"`                                                                                                                                             |
| `listener` | (`event`: `Event`, `launchInfo`: `Record`\<`string`, `any`\> \| [`NotificationResponse`](/lynxtron/api/Interface.NotificationResponse.mdx)) => `void` |

##### 返回

`this`

##### 添加于

v10.0.0

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:825

#### off(event, listener)

```ts
off(event: "second-instance", listener: (event: Event, argv: string[], workingDirectory: string, additionalData: unknown) => void): this
```

Alias for `emitter.removeListener()`.

##### 参数

| 范围       | 类型                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------- |
| `event`    | `"second-instance"`                                                                                         |
| `listener` | (`event`: `Event`, `argv`: `string`[], `workingDirectory`: `string`, `additionalData`: `unknown`) => `void` |

##### 返回

`this`

##### 添加于

v10.0.0

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:906

#### off(event, listener)

```ts
off(event: "update-activity-state", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"update-activity-state"`                                             |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:1005

#### off(event, listener)

```ts
off(event: "will-continue-activity", listener: (event: Event, type: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"will-continue-activity"`                     |
| `listener` | (`event`: `Event`, `type`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:1091

#### off(event, listener)

```ts
off(event: "will-finish-launching", listener: () => void): this
```

Alias for `emitter.removeListener()`.

##### 参数

| 范围       | 类型                      |
| ---------- | ------------------------- |
| `event`    | `"will-finish-launching"` |
| `listener` | () => `void`              |

##### 返回

`this`

##### 添加于

v10.0.0

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:1149

#### off(event, listener)

```ts
off(event: "will-quit", listener: (event: Event) => void): this
```

Alias for `emitter.removeListener()`.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"will-quit"`                |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 添加于

v10.0.0

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:1165

#### off(event, listener)

```ts
off(event: "window-all-closed", listener: () => void): this
```

Alias for `emitter.removeListener()`.

##### 参数

| 范围       | 类型                  |
| ---------- | --------------------- |
| `event`    | `"window-all-closed"` |
| `listener` | () => `void`          |

##### 返回

`this`

##### 添加于

v10.0.0

##### 重写了

`EventEmitter.off`

##### 定义于

lynxtron/apis/api/app.d.ts:1180

---

### on()

#### on(event, listener)

```ts
on(event: "activate", listener: (event: Event, hasVisibleWindows: boolean) => void): this
```

Emitted when the application is activated. Various actions can trigger this
event, such as launching the application for the first time, attempting to
re-launch the application when it's already running, or clicking on the
application's dock or taskbar icon.

##### 参数

| 范围       | 类型                                                         |
| ---------- | ------------------------------------------------------------ |
| `event`    | `"activate"`                                                 |
| `listener` | (`event`: `Event`, `hasVisibleWindows`: `boolean`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:293

#### on(event, listener)

```ts
on(event: "activity-was-continued", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

Emitted during Handoff after an activity from this device was successfully
resumed on another one.

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"activity-was-continued"`                                            |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:331

#### on(event, listener)

```ts
on(event: "before-quit", listener: (event: Event) => void): this
```

Emitted before the application starts closing its windows. Calling
`event.preventDefault()` will prevent the default behavior, which is terminating
the application.

[!NOTE] If application quit was initiated by `autoUpdater.quitAndInstall()`,
then `before-quit` is emitted _after_ emitting `close` event on all windows and
closing them.

[!NOTE] On Windows, this event will not be emitted if the app is closed due to
a shutdown/restart of the system or a user logout.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"before-quit"`              |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:425

#### on(event, listener)

```ts
on(event: "continue-activity", listener: (event: Event, type: string, userInfo: unknown, details: ContinueActivityDetails) => void): this
```

Emitted during Handoff when an activity from a different device wants to be
resumed. You should call `event.preventDefault()` if you want to handle this
event.

A user activity can be continued only in an app that has the same developer Team
ID as the activity's source app and that supports the activity's type. Supported
activity types are specified in the app's `Info.plist` under the
`NSUserActivityTypes` key.

##### 参数

| 范围       | 类型                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `event`    | `"continue-activity"`                                                                                                                                              |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`, `details`: [`ContinueActivityDetails`](/lynxtron/api/Interface.ContinueActivityDetails.mdx)) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:443

#### on(event, listener)

```ts
on(event: "continue-activity-error", listener: (event: Event, type: string, error: string) => void): this
```

Emitted during Handoff when an activity from a different device fails to be
resumed.

##### 参数

| 范围       | 类型                                                              |
| ---------- | ----------------------------------------------------------------- |
| `event`    | `"continue-activity-error"`                                       |
| `listener` | (`event`: `Event`, `type`: `string`, `error`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:536

#### on(event, listener)

```ts
on(event: "did-become-active", listener: (event: Event) => void): this
```

Emitted when the application becomes active. This differs from the `activate`
event in that `did-become-active` is emitted every time the app becomes active,
not only when Dock icon is clicked or application is re-launched. It is also
emitted when a user switches to the app via the macOS App Switcher.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-become-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:626

#### on(event, listener)

```ts
on(event: "did-resign-active", listener: (event: Event) => void): this
```

Emitted when the app is no longer active and doesn’t have focus. This can be
triggered, for example, by clicking on another application or by using the macOS
App Switcher to switch to another application.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-resign-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:656

#### on(event, listener)

```ts
on(event: "new-window-for-tab", listener: (event: Event) => void): this
```

Emitted when the user clicks the native macOS new tab button. The new tab button
is only visible if the current `BrowserWindow` has a `tabbingIdentifier`

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"new-window-for-tab"`       |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:686

#### on(event, listener)

```ts
on(event: "open-file", listener: (event: Event, path: string) => void): this
```

Emitted when the user wants to open a file with the application. The `open-file`
event is usually emitted when the application is already open and the OS wants
to reuse the application to open the file. `open-file` is also emitted when a
file is dropped onto the dock and the application is not yet running. Make sure
to listen for the `open-file` event very early in your application startup to
handle this case (even before the `ready` event is emitted).

You should call `event.preventDefault()` if you want to handle this event.

On Windows, you have to parse `process.argv` (in the main process) to get the
filepath.

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"open-file"`                                  |
| `listener` | (`event`: `Event`, `path`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:724

#### on(event, listener)

```ts
on(event: "open-url", listener: (event: Event, url: string) => void): this
```

Emitted when the user wants to open a URL with the application. Your
application's `Info.plist` file must define the URL scheme within the
`CFBundleURLTypes` key, and set `NSPrincipalClass` to `AtomApplication`.

As with the `open-file` event, be sure to register a listener for the `open-url`
event early in your application startup to detect if the application is being
opened to handle a URL. If you register the listener in response to a `ready`
event, you'll miss URLs that trigger the launch of your application.

##### 参数

| 范围       | 类型                                          |
| ---------- | --------------------------------------------- |
| `event`    | `"open-url"`                                  |
| `listener` | (`event`: `Event`, `url`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:762

#### on(event, listener)

```ts
on(event: "quit", listener: (event: Event, exitCode: number) => void): this
```

Emitted when the application is quitting.

[!NOTE] On Windows, this event will not be emitted if the app is closed due to
a shutdown/restart of the system or a user logout.

##### 参数

| 范围       | 类型                                               |
| ---------- | -------------------------------------------------- |
| `event`    | `"quit"`                                           |
| `listener` | (`event`: `Event`, `exitCode`: `number`) => `void` |

##### 返回

`this`

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:791

#### on(event, listener)

```ts
on(event: "ready", listener: (event: Event, launchInfo: Record<string, any> | NotificationResponse) => void): this
```

Emitted once, when Electron has finished initializing. On macOS, `launchInfo`
holds the `userInfo` of the `NSUserNotification` or information from
`UNNotificationResponse` that was used to open the application, if it was
launched from Notification Center. You can also call `app.isReady()` to check if
this event has already fired and `app.whenReady()` to get a Promise that is
fulfilled when Electron is initialized.

[!NOTE] The `ready` event is only fired after the main process has finished
running the first tick of the event loop. If an Electron API needs to be called
before the `ready` event, ensure that it is called synchronously in the
top-level context of the main process.

##### 参数

| 范围       | 类型                                                                                                                                                  |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `event`    | `"ready"`                                                                                                                                             |
| `listener` | (`event`: `Event`, `launchInfo`: `Record`\<`string`, `any`\> \| [`NotificationResponse`](/lynxtron/api/Interface.NotificationResponse.mdx)) => `void` |

##### 返回

`this`

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:815

#### on(event, listener)

```ts
on(event: "second-instance", listener: (event: Event, argv: string[], workingDirectory: string, additionalData: unknown) => void): this
```

This event will be emitted inside the primary instance of your application when
a second instance has been executed and calls `app.requestSingleInstanceLock()`.

`argv` is an Array of the second instance's command line arguments, and
`workingDirectory` is its current working directory. Usually applications
respond to this by making their primary window focused and non-minimized.

[!NOTE] `argv` will not be exactly the same list of arguments as those passed
to the second instance. The order might change and additional arguments might be
appended. If you need to maintain the exact same arguments, it's advised to use
`additionalData` instead.

[!NOTE] If the second instance is started by a different user than the first,
the `argv` array will not include the arguments.

This event is guaranteed to be emitted after the `ready` event of `app` gets
emitted.

[!NOTE] Extra command line arguments might be added by Chromium, such as
`--original-process-start-time`.

##### 参数

| 范围       | 类型                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------- |
| `event`    | `"second-instance"`                                                                                         |
| `listener` | (`event`: `Event`, `argv`: `string`[], `workingDirectory`: `string`, `additionalData`: `unknown`) => `void` |

##### 返回

`this`

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:888

#### on(event, listener)

```ts
on(event: "update-activity-state", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

Emitted when Handoff is about to be resumed on another device. If you need to
update the state to be transferred, you should call `event.preventDefault()`
immediately, construct a new `userInfo` dictionary and call
`app.updateCurrentActivity()` in a timely manner. Otherwise, the operation will
fail and `continue-activity-error` will be called.

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"update-activity-state"`                                             |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:988

#### on(event, listener)

```ts
on(event: "will-continue-activity", listener: (event: Event, type: string) => void): this
```

Emitted during Handoff before an activity from a different device wants to be
resumed. You should call `event.preventDefault()` if you want to handle this
event.

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"will-continue-activity"`                     |
| `listener` | (`event`: `Event`, `type`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:1078

#### on(event, listener)

```ts
on(event: "will-finish-launching", listener: () => void): this
```

Emitted when the application has finished basic startup. On Windows and Linux,
the `will-finish-launching` event is the same as the `ready` event; on macOS,
this event represents the `applicationWillFinishLaunching` notification of
`NSApplication`.

In most cases, you should do everything in the `ready` event handler.

##### 参数

| 范围       | 类型                      |
| ---------- | ------------------------- |
| `event`    | `"will-finish-launching"` |
| `listener` | () => `void`              |

##### 返回

`this`

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:1148

#### on(event, listener)

```ts
on(event: "will-quit", listener: (event: Event) => void): this
```

Emitted when all windows have been closed and the application will quit. Calling
`event.preventDefault()` will prevent the default behavior, which is terminating
the application.

See the description of the `window-all-closed` event for the differences between
the `will-quit` and `window-all-closed` events.

[!NOTE] On Windows, this event will not be emitted if the app is closed due to
a shutdown/restart of the system or a user logout.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"will-quit"`                |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:1164

#### on(event, listener)

```ts
on(event: "window-all-closed", listener: () => void): this
```

Emitted when all windows have been closed.

If you do not subscribe to this event and all windows are closed, the default
behavior is to quit the app; however, if you subscribe, you control whether the
app quits or not. If the user pressed `Cmd + Q`, or the developer called
`app.quit()`, Electron will first try to close all the windows and then emit the
`will-quit` event, and in this case the `window-all-closed` event would not be
emitted.

##### 参数

| 范围       | 类型                  |
| ---------- | --------------------- |
| `event`    | `"window-all-closed"` |
| `listener` | () => `void`          |

##### 返回

`this`

##### 重写了

`EventEmitter.on`

##### 定义于

lynxtron/apis/api/app.d.ts:1179

---

### once()

#### once(event, listener)

```ts
once(event: "activate", listener: (event: Event, hasVisibleWindows: boolean) => void): this
```

##### 参数

| 范围       | 类型                                                         |
| ---------- | ------------------------------------------------------------ |
| `event`    | `"activate"`                                                 |
| `listener` | (`event`: `Event`, `hasVisibleWindows`: `boolean`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:307

#### once(event, listener)

```ts
once(event: "activity-was-continued", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"activity-was-continued"`                                            |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:365

#### once(event, listener)

```ts
once(event: "before-quit", listener: (event: Event) => void): this
```

Adds a **one-time**`listener` function for the event named `eventName`. The
next time `eventName` is triggered, this listener is removed and then invoked.

```js
server.once('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

By default, event listeners are invoked in the order they are added. The`emitter.prependOnceListener()` method can be used as an alternative to add the
event listener to the beginning of the listeners array.

```js
const myEE = new EventEmitter();
myEE.once('foo', () => console.log('a'));
myEE.prependOnceListener('foo', () => console.log('b'));
myEE.emit('foo');
// Prints:
//   b
//   a
```

##### 参数

| 范围       | 类型                         | 描述                  |
| ---------- | ---------------------------- | --------------------- |
| `event`    | `"before-quit"`              | -                     |
| `listener` | (`event`: `Event`) => `void` | The callback function |

##### 返回

`this`

##### 添加于

v0.3.0

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:427

#### once(event, listener)

```ts
once(event: "continue-activity", listener: (event: Event, type: string, userInfo: unknown, details: ContinueActivityDetails) => void): this
```

##### 参数

| 范围       | 类型                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `event`    | `"continue-activity"`                                                                                                                                              |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`, `details`: [`ContinueActivityDetails`](/lynxtron/api/Interface.ContinueActivityDetails.mdx)) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:479

#### once(event, listener)

```ts
once(event: "continue-activity-error", listener: (event: Event, type: string, error: string) => void): this
```

##### 参数

| 范围       | 类型                                                              |
| ---------- | ----------------------------------------------------------------- |
| `event`    | `"continue-activity-error"`                                       |
| `listener` | (`event`: `Event`, `type`: `string`, `error`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:570

#### once(event, listener)

```ts
once(event: "did-become-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-become-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:634

#### once(event, listener)

```ts
once(event: "did-resign-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-resign-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:664

#### once(event, listener)

```ts
once(event: "new-window-for-tab", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"new-window-for-tab"`       |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:694

#### once(event, listener)

```ts
once(event: "open-file", listener: (event: Event, path: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"open-file"`                                  |
| `listener` | (`event`: `Event`, `path`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:732

#### once(event, listener)

```ts
once(event: "open-url", listener: (event: Event, url: string) => void): this
```

##### 参数

| 范围       | 类型                                          |
| ---------- | --------------------------------------------- |
| `event`    | `"open-url"`                                  |
| `listener` | (`event`: `Event`, `url`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:770

#### once(event, listener)

```ts
once(event: "quit", listener: (event: Event, exitCode: number) => void): this
```

Adds a **one-time**`listener` function for the event named `eventName`. The
next time `eventName` is triggered, this listener is removed and then invoked.

```js
server.once('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

By default, event listeners are invoked in the order they are added. The`emitter.prependOnceListener()` method can be used as an alternative to add the
event listener to the beginning of the listeners array.

```js
const myEE = new EventEmitter();
myEE.once('foo', () => console.log('a'));
myEE.prependOnceListener('foo', () => console.log('b'));
myEE.emit('foo');
// Prints:
//   b
//   a
```

##### 参数

| 范围       | 类型                                               | 描述                  |
| ---------- | -------------------------------------------------- | --------------------- |
| `event`    | `"quit"`                                           | -                     |
| `listener` | (`event`: `Event`, `exitCode`: `number`) => `void` | The callback function |

##### 返回

`this`

##### 添加于

v0.3.0

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:793

#### once(event, listener)

```ts
once(event: "ready", listener: (event: Event, launchInfo: Record<string, any> | NotificationResponse) => void): this
```

Adds a **one-time**`listener` function for the event named `eventName`. The
next time `eventName` is triggered, this listener is removed and then invoked.

```js
server.once('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

By default, event listeners are invoked in the order they are added. The`emitter.prependOnceListener()` method can be used as an alternative to add the
event listener to the beginning of the listeners array.

```js
const myEE = new EventEmitter();
myEE.once('foo', () => console.log('a'));
myEE.prependOnceListener('foo', () => console.log('b'));
myEE.emit('foo');
// Prints:
//   b
//   a
```

##### 参数

| 范围       | 类型                                                                                                                                                  | 描述                  |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| `event`    | `"ready"`                                                                                                                                             | -                     |
| `listener` | (`event`: `Event`, `launchInfo`: `Record`\<`string`, `any`\> \| [`NotificationResponse`](/lynxtron/api/Interface.NotificationResponse.mdx)) => `void` | The callback function |

##### 返回

`this`

##### 添加于

v0.3.0

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:835

#### once(event, listener)

```ts
once(event: "second-instance", listener: (event: Event, argv: string[], workingDirectory: string, additionalData: unknown) => void): this
```

Adds a **one-time**`listener` function for the event named `eventName`. The
next time `eventName` is triggered, this listener is removed and then invoked.

```js
server.once('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

By default, event listeners are invoked in the order they are added. The`emitter.prependOnceListener()` method can be used as an alternative to add the
event listener to the beginning of the listeners array.

```js
const myEE = new EventEmitter();
myEE.once('foo', () => console.log('a'));
myEE.prependOnceListener('foo', () => console.log('b'));
myEE.emit('foo');
// Prints:
//   b
//   a
```

##### 参数

| 范围       | 类型                                                                                                        | 描述                  |
| ---------- | ----------------------------------------------------------------------------------------------------------- | --------------------- |
| `event`    | `"second-instance"`                                                                                         | -                     |
| `listener` | (`event`: `Event`, `argv`: `string`[], `workingDirectory`: `string`, `additionalData`: `unknown`) => `void` | The callback function |

##### 返回

`this`

##### 添加于

v0.3.0

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:924

#### once(event, listener)

```ts
once(event: "update-activity-state", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"update-activity-state"`                                             |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:1022

#### once(event, listener)

```ts
once(event: "will-continue-activity", listener: (event: Event, type: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"will-continue-activity"`                     |
| `listener` | (`event`: `Event`, `type`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:1104

#### once(event, listener)

```ts
once(event: "will-finish-launching", listener: () => void): this
```

Adds a **one-time**`listener` function for the event named `eventName`. The
next time `eventName` is triggered, this listener is removed and then invoked.

```js
server.once('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

By default, event listeners are invoked in the order they are added. The`emitter.prependOnceListener()` method can be used as an alternative to add the
event listener to the beginning of the listeners array.

```js
const myEE = new EventEmitter();
myEE.once('foo', () => console.log('a'));
myEE.prependOnceListener('foo', () => console.log('b'));
myEE.emit('foo');
// Prints:
//   b
//   a
```

##### 参数

| 范围       | 类型                      | 描述                  |
| ---------- | ------------------------- | --------------------- |
| `event`    | `"will-finish-launching"` | -                     |
| `listener` | () => `void`              | The callback function |

##### 返回

`this`

##### 添加于

v0.3.0

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:1150

#### once(event, listener)

```ts
once(event: "will-quit", listener: (event: Event) => void): this
```

Adds a **one-time**`listener` function for the event named `eventName`. The
next time `eventName` is triggered, this listener is removed and then invoked.

```js
server.once('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

By default, event listeners are invoked in the order they are added. The`emitter.prependOnceListener()` method can be used as an alternative to add the
event listener to the beginning of the listeners array.

```js
const myEE = new EventEmitter();
myEE.once('foo', () => console.log('a'));
myEE.prependOnceListener('foo', () => console.log('b'));
myEE.emit('foo');
// Prints:
//   b
//   a
```

##### 参数

| 范围       | 类型                         | 描述                  |
| ---------- | ---------------------------- | --------------------- |
| `event`    | `"will-quit"`                | -                     |
| `listener` | (`event`: `Event`) => `void` | The callback function |

##### 返回

`this`

##### 添加于

v0.3.0

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:1166

#### once(event, listener)

```ts
once(event: "window-all-closed", listener: () => void): this
```

Adds a **one-time**`listener` function for the event named `eventName`. The
next time `eventName` is triggered, this listener is removed and then invoked.

```js
server.once('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

By default, event listeners are invoked in the order they are added. The`emitter.prependOnceListener()` method can be used as an alternative to add the
event listener to the beginning of the listeners array.

```js
const myEE = new EventEmitter();
myEE.once('foo', () => console.log('a'));
myEE.prependOnceListener('foo', () => console.log('b'));
myEE.emit('foo');
// Prints:
//   b
//   a
```

##### 参数

| 范围       | 类型                  | 描述                  |
| ---------- | --------------------- | --------------------- |
| `event`    | `"window-all-closed"` | -                     |
| `listener` | () => `void`          | The callback function |

##### 返回

`this`

##### 添加于

v0.3.0

##### 重写了

`EventEmitter.once`

##### 定义于

lynxtron/apis/api/app.d.ts:1181

---

### prependListener()

```ts
prependListener<K>(eventName: string | symbol, listener: (...args: any[]) => void): this
```

Adds the `listener` function to the _beginning_ of the listeners array for the
event named `eventName`. No checks are made to see if the `listener` has
already been added. Multiple calls passing the same combination of `eventName` and `listener` will result in the `listener` being added, and called, multiple
times.

```js
server.prependListener('connection', (stream) => {
  console.log('someone connected!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

#### 类型参数

| 类型参数 |
| -------- |
| `K`      |

#### 参数

| 范围        | 类型                           | 描述                   |
| ----------- | ------------------------------ | ---------------------- |
| `eventName` | `string` \| `symbol`           | The name of the event. |
| `listener`  | (...`args`: `any`[]) => `void` | The callback function  |

#### 返回

`this`

#### 添加于

v6.0.0

#### 继承自

`EventEmitter.prependListener`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:775

---

### prependOnceListener()

```ts
prependOnceListener<K>(eventName: string | symbol, listener: (...args: any[]) => void): this
```

Adds a **one-time**`listener` function for the event named `eventName` to the _beginning_ of the listeners array. The next time `eventName` is triggered, this
listener is removed, and then invoked.

```js
server.prependOnceListener('connection', (stream) => {
  console.log('Ah, we have our first user!');
});
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

#### 类型参数

| 类型参数 |
| -------- |
| `K`      |

#### 参数

| 范围        | 类型                           | 描述                   |
| ----------- | ------------------------------ | ---------------------- |
| `eventName` | `string` \| `symbol`           | The name of the event. |
| `listener`  | (...`args`: `any`[]) => `void` | The callback function  |

#### 返回

`this`

#### 添加于

v6.0.0

#### 继承自

`EventEmitter.prependOnceListener`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:791

---

### quit()

```ts
quit(): void
```

Try to close all windows. The `before-quit` event will be emitted first. If all
windows are successfully closed, the `will-quit` event will be emitted and by
default the application will terminate.

This method guarantees that all `beforeunload` and `unload` event handlers are
correctly executed. It is possible that a window cancels the quitting by
returning `false` in the `beforeunload` event handler.

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1463

---

### rawListeners()

```ts
rawListeners<K>(eventName: string | symbol): Function[]
```

Returns a copy of the array of listeners for the event named `eventName`,
including any wrappers (such as those created by `.once()`).

```js
const emitter = new EventEmitter();
emitter.once('log', () => console.log('log once'));

// Returns a new Array with a function `onceWrapper` which has a property
// `listener` which contains the original listener bound above
const listeners = emitter.rawListeners('log');
const logFnWrapper = listeners[0];

// Logs "log once" to the console and does not unbind the `once` event
logFnWrapper.listener();

// Logs "log once" to the console and removes the listener
logFnWrapper();

emitter.on('log', () => console.log('log persistently'));
// Will return a new Array with a single function bound by `.on()` above
const newListeners = emitter.rawListeners('log');

// Logs "log persistently" twice
newListeners[0]();
emitter.emit('log');
```

#### 类型参数

| 类型参数 |
| -------- |
| `K`      |

#### 参数

| 范围        | 类型                 |
| ----------- | -------------------- |
| `eventName` | `string` \| `symbol` |

#### 返回

`Function`[]

#### 添加于

v9.4.0

#### 继承自

`EventEmitter.rawListeners`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:706

---

### relaunch()

```ts
relaunch(options?: RelaunchOptions): void
```

Relaunches the app when the current instance exits.

By default, the new instance will use the same working directory and command
line arguments as the current instance. When `args` is specified, the `args`
will be passed as the command line arguments instead. When `execPath` is
specified, the `execPath` will be executed for the relaunch instead of the
current app.

Note that this method does not quit the app when executed. You have to call
`app.quit` or `app.exit` after calling `app.relaunch` to make the app restart.

When `app.relaunch` is called multiple times, multiple instances will be started
after the current instance exits.

An example of restarting the current instance immediately and adding a new
command line argument to the new instance:

#### 参数

| 范围       | 类型                                                             |
| ---------- | ---------------------------------------------------------------- |
| `options`? | [`RelaunchOptions`](/lynxtron/api/Interface.RelaunchOptions.mdx) |

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1482

---

### releaseSingleInstanceLock()

```ts
releaseSingleInstanceLock(): void
```

Releases all locks that were created by `requestSingleInstanceLock`. This will
allow multiple instances of the application to once again run side by side.

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1487

---

### removeAllListeners()

```ts
removeAllListeners(event?: string | symbol): this
```

Removes all listeners, or those of the specified `eventName`.

It is bad practice to remove listeners added elsewhere in the code,
particularly when the `EventEmitter` instance was created by some other
component or module (e.g. sockets or file streams).

Returns a reference to the `EventEmitter`, so that calls can be chained.

#### 参数

| 范围     | 类型                 |
| -------- | -------------------- |
| `event`? | `string` \| `symbol` |

#### 返回

`this`

#### 添加于

v0.1.26

#### 继承自

`EventEmitter.removeAllListeners`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:647

---

### removeAsDefaultProtocolClient()

```ts
removeAsDefaultProtocolClient(
   protocol: string,
   path?: string,
   args?: string[]): boolean
```

Whether the call succeeded.

This method checks if the current executable as the default handler for a
protocol (aka URI scheme). If so, it will remove the app as the default handler.

#### 参数

| 范围       | 类型       |
| ---------- | ---------- |
| `protocol` | `string`   |
| `path`?    | `string`   |
| `args`?    | `string`[] |

#### 返回

`boolean`

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1496

---

### removeListener()

#### removeListener(event, listener)

```ts
removeListener(event: "activate", listener: (event: Event, hasVisibleWindows: boolean) => void): this
```

##### 参数

| 范围       | 类型                                                         |
| ---------- | ------------------------------------------------------------ |
| `event`    | `"activate"`                                                 |
| `listener` | (`event`: `Event`, `hasVisibleWindows`: `boolean`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:321

#### removeListener(event, listener)

```ts
removeListener(event: "activity-was-continued", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"activity-was-continued"`                                            |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:399

#### removeListener(event, listener)

```ts
removeListener(event: "before-quit", listener: (event: Event) => void): this
```

Removes the specified `listener` from the listener array for the event named`eventName`.

```js
const callback = (stream) => {
  console.log('someone connected!');
};
server.on('connection', callback);
// ...
server.removeListener('connection', callback);
```

`removeListener()` will remove, at most, one instance of a listener from the
listener array. If any single listener has been added multiple times to the
listener array for the specified `eventName`, then `removeListener()` must be
called multiple times to remove each instance.

Once an event is emitted, all listeners attached to it at the
time of emitting are called in order. This implies that any`removeListener()` or `removeAllListeners()` calls _after_ emitting and _before_ the last listener finishes execution
will not remove them from`emit()` in progress. Subsequent events behave as expected.

```js
const myEmitter = new MyEmitter();

const callbackA = () => {
  console.log('A');
  myEmitter.removeListener('event', callbackB);
};

const callbackB = () => {
  console.log('B');
};

myEmitter.on('event', callbackA);

myEmitter.on('event', callbackB);

// callbackA removes listener callbackB but it will still be called.
// Internal listener array at time of emit [callbackA, callbackB]
myEmitter.emit('event');
// Prints:
//   A
//   B

// callbackB is now removed.
// Internal listener array [callbackA]
myEmitter.emit('event');
// Prints:
//   A
```

Because listeners are managed using an internal array, calling this will
change the position indices of any listener registered _after_ the listener
being removed. This will not impact the order in which listeners are called,
but it means that any copies of the listener array as returned by
the `emitter.listeners()` method will need to be recreated.

When a single function has been added as a handler multiple times for a single
event (as in the example below), `removeListener()` will remove the most
recently added instance. In the example the `once('ping')`listener is removed:

```js
const ee = new EventEmitter();

function pong() {
  console.log('pong');
}

ee.on('ping', pong);
ee.once('ping', pong);
ee.removeListener('ping', pong);

ee.emit('ping');
ee.emit('ping');
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"before-quit"`              |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:429

#### removeListener(event, listener)

```ts
removeListener(event: "continue-activity", listener: (event: Event, type: string, userInfo: unknown, details: ContinueActivityDetails) => void): this
```

##### 参数

| 范围       | 类型                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `event`    | `"continue-activity"`                                                                                                                                              |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`, `details`: [`ContinueActivityDetails`](/lynxtron/api/Interface.ContinueActivityDetails.mdx)) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:515

#### removeListener(event, listener)

```ts
removeListener(event: "continue-activity-error", listener: (event: Event, type: string, error: string) => void): this
```

##### 参数

| 范围       | 类型                                                              |
| ---------- | ----------------------------------------------------------------- |
| `event`    | `"continue-activity-error"`                                       |
| `listener` | (`event`: `Event`, `type`: `string`, `error`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:604

#### removeListener(event, listener)

```ts
removeListener(event: "did-become-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-become-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:645

#### removeListener(event, listener)

```ts
removeListener(event: "did-resign-active", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"did-resign-active"`        |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:675

#### removeListener(event, listener)

```ts
removeListener(event: "new-window-for-tab", listener: (event: Event) => void): this
```

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"new-window-for-tab"`       |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:705

#### removeListener(event, listener)

```ts
removeListener(event: "open-file", listener: (event: Event, path: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"open-file"`                                  |
| `listener` | (`event`: `Event`, `path`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:746

#### removeListener(event, listener)

```ts
removeListener(event: "open-url", listener: (event: Event, url: string) => void): this
```

##### 参数

| 范围       | 类型                                          |
| ---------- | --------------------------------------------- |
| `event`    | `"open-url"`                                  |
| `listener` | (`event`: `Event`, `url`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:781

#### removeListener(event, listener)

```ts
removeListener(event: "quit", listener: (event: Event, exitCode: number) => void): this
```

Removes the specified `listener` from the listener array for the event named`eventName`.

```js
const callback = (stream) => {
  console.log('someone connected!');
};
server.on('connection', callback);
// ...
server.removeListener('connection', callback);
```

`removeListener()` will remove, at most, one instance of a listener from the
listener array. If any single listener has been added multiple times to the
listener array for the specified `eventName`, then `removeListener()` must be
called multiple times to remove each instance.

Once an event is emitted, all listeners attached to it at the
time of emitting are called in order. This implies that any`removeListener()` or `removeAllListeners()` calls _after_ emitting and _before_ the last listener finishes execution
will not remove them from`emit()` in progress. Subsequent events behave as expected.

```js
const myEmitter = new MyEmitter();

const callbackA = () => {
  console.log('A');
  myEmitter.removeListener('event', callbackB);
};

const callbackB = () => {
  console.log('B');
};

myEmitter.on('event', callbackA);

myEmitter.on('event', callbackB);

// callbackA removes listener callbackB but it will still be called.
// Internal listener array at time of emit [callbackA, callbackB]
myEmitter.emit('event');
// Prints:
//   A
//   B

// callbackB is now removed.
// Internal listener array [callbackA]
myEmitter.emit('event');
// Prints:
//   A
```

Because listeners are managed using an internal array, calling this will
change the position indices of any listener registered _after_ the listener
being removed. This will not impact the order in which listeners are called,
but it means that any copies of the listener array as returned by
the `emitter.listeners()` method will need to be recreated.

When a single function has been added as a handler multiple times for a single
event (as in the example below), `removeListener()` will remove the most
recently added instance. In the example the `once('ping')`listener is removed:

```js
const ee = new EventEmitter();

function pong() {
  console.log('pong');
}

ee.on('ping', pong);
ee.once('ping', pong);
ee.removeListener('ping', pong);

ee.emit('ping');
ee.emit('ping');
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

##### 参数

| 范围       | 类型                                               |
| ---------- | -------------------------------------------------- |
| `event`    | `"quit"`                                           |
| `listener` | (`event`: `Event`, `exitCode`: `number`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:798

#### removeListener(event, listener)

```ts
removeListener(event: "ready", listener: (event: Event, launchInfo: Record<string, any> | NotificationResponse) => void): this
```

Removes the specified `listener` from the listener array for the event named`eventName`.

```js
const callback = (stream) => {
  console.log('someone connected!');
};
server.on('connection', callback);
// ...
server.removeListener('connection', callback);
```

`removeListener()` will remove, at most, one instance of a listener from the
listener array. If any single listener has been added multiple times to the
listener array for the specified `eventName`, then `removeListener()` must be
called multiple times to remove each instance.

Once an event is emitted, all listeners attached to it at the
time of emitting are called in order. This implies that any`removeListener()` or `removeAllListeners()` calls _after_ emitting and _before_ the last listener finishes execution
will not remove them from`emit()` in progress. Subsequent events behave as expected.

```js
const myEmitter = new MyEmitter();

const callbackA = () => {
  console.log('A');
  myEmitter.removeListener('event', callbackB);
};

const callbackB = () => {
  console.log('B');
};

myEmitter.on('event', callbackA);

myEmitter.on('event', callbackB);

// callbackA removes listener callbackB but it will still be called.
// Internal listener array at time of emit [callbackA, callbackB]
myEmitter.emit('event');
// Prints:
//   A
//   B

// callbackB is now removed.
// Internal listener array [callbackA]
myEmitter.emit('event');
// Prints:
//   A
```

Because listeners are managed using an internal array, calling this will
change the position indices of any listener registered _after_ the listener
being removed. This will not impact the order in which listeners are called,
but it means that any copies of the listener array as returned by
the `emitter.listeners()` method will need to be recreated.

When a single function has been added as a handler multiple times for a single
event (as in the example below), `removeListener()` will remove the most
recently added instance. In the example the `once('ping')`listener is removed:

```js
const ee = new EventEmitter();

function pong() {
  console.log('pong');
}

ee.on('ping', pong);
ee.once('ping', pong);
ee.removeListener('ping', pong);

ee.emit('ping');
ee.emit('ping');
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

##### 参数

| 范围       | 类型                                                                                                                                                  |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `event`    | `"ready"`                                                                                                                                             |
| `listener` | (`event`: `Event`, `launchInfo`: `Record`\<`string`, `any`\> \| [`NotificationResponse`](/lynxtron/api/Interface.NotificationResponse.mdx)) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:855

#### removeListener(event, listener)

```ts
removeListener(event: "second-instance", listener: (event: Event, argv: string[], workingDirectory: string, additionalData: unknown) => void): this
```

Removes the specified `listener` from the listener array for the event named`eventName`.

```js
const callback = (stream) => {
  console.log('someone connected!');
};
server.on('connection', callback);
// ...
server.removeListener('connection', callback);
```

`removeListener()` will remove, at most, one instance of a listener from the
listener array. If any single listener has been added multiple times to the
listener array for the specified `eventName`, then `removeListener()` must be
called multiple times to remove each instance.

Once an event is emitted, all listeners attached to it at the
time of emitting are called in order. This implies that any`removeListener()` or `removeAllListeners()` calls _after_ emitting and _before_ the last listener finishes execution
will not remove them from`emit()` in progress. Subsequent events behave as expected.

```js
const myEmitter = new MyEmitter();

const callbackA = () => {
  console.log('A');
  myEmitter.removeListener('event', callbackB);
};

const callbackB = () => {
  console.log('B');
};

myEmitter.on('event', callbackA);

myEmitter.on('event', callbackB);

// callbackA removes listener callbackB but it will still be called.
// Internal listener array at time of emit [callbackA, callbackB]
myEmitter.emit('event');
// Prints:
//   A
//   B

// callbackB is now removed.
// Internal listener array [callbackA]
myEmitter.emit('event');
// Prints:
//   A
```

Because listeners are managed using an internal array, calling this will
change the position indices of any listener registered _after_ the listener
being removed. This will not impact the order in which listeners are called,
but it means that any copies of the listener array as returned by
the `emitter.listeners()` method will need to be recreated.

When a single function has been added as a handler multiple times for a single
event (as in the example below), `removeListener()` will remove the most
recently added instance. In the example the `once('ping')`listener is removed:

```js
const ee = new EventEmitter();

function pong() {
  console.log('pong');
}

ee.on('ping', pong);
ee.once('ping', pong);
ee.removeListener('ping', pong);

ee.emit('ping');
ee.emit('ping');
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

##### 参数

| 范围       | 类型                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------- |
| `event`    | `"second-instance"`                                                                                         |
| `listener` | (`event`: `Event`, `argv`: `string`[], `workingDirectory`: `string`, `additionalData`: `unknown`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:960

#### removeListener(event, listener)

```ts
removeListener(event: "update-activity-state", listener: (event: Event, type: string, userInfo: unknown) => void): this
```

##### 参数

| 范围       | 类型                                                                  |
| ---------- | --------------------------------------------------------------------- |
| `event`    | `"update-activity-state"`                                             |
| `listener` | (`event`: `Event`, `type`: `string`, `userInfo`: `unknown`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1056

#### removeListener(event, listener)

```ts
removeListener(event: "will-continue-activity", listener: (event: Event, type: string) => void): this
```

##### 参数

| 范围       | 类型                                           |
| ---------- | ---------------------------------------------- |
| `event`    | `"will-continue-activity"`                     |
| `listener` | (`event`: `Event`, `type`: `string`) => `void` |

##### 返回

`this`

##### Platform

darwin

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1130

#### removeListener(event, listener)

```ts
removeListener(event: "will-finish-launching", listener: () => void): this
```

Removes the specified `listener` from the listener array for the event named`eventName`.

```js
const callback = (stream) => {
  console.log('someone connected!');
};
server.on('connection', callback);
// ...
server.removeListener('connection', callback);
```

`removeListener()` will remove, at most, one instance of a listener from the
listener array. If any single listener has been added multiple times to the
listener array for the specified `eventName`, then `removeListener()` must be
called multiple times to remove each instance.

Once an event is emitted, all listeners attached to it at the
time of emitting are called in order. This implies that any`removeListener()` or `removeAllListeners()` calls _after_ emitting and _before_ the last listener finishes execution
will not remove them from`emit()` in progress. Subsequent events behave as expected.

```js
const myEmitter = new MyEmitter();

const callbackA = () => {
  console.log('A');
  myEmitter.removeListener('event', callbackB);
};

const callbackB = () => {
  console.log('B');
};

myEmitter.on('event', callbackA);

myEmitter.on('event', callbackB);

// callbackA removes listener callbackB but it will still be called.
// Internal listener array at time of emit [callbackA, callbackB]
myEmitter.emit('event');
// Prints:
//   A
//   B

// callbackB is now removed.
// Internal listener array [callbackA]
myEmitter.emit('event');
// Prints:
//   A
```

Because listeners are managed using an internal array, calling this will
change the position indices of any listener registered _after_ the listener
being removed. This will not impact the order in which listeners are called,
but it means that any copies of the listener array as returned by
the `emitter.listeners()` method will need to be recreated.

When a single function has been added as a handler multiple times for a single
event (as in the example below), `removeListener()` will remove the most
recently added instance. In the example the `once('ping')`listener is removed:

```js
const ee = new EventEmitter();

function pong() {
  console.log('pong');
}

ee.on('ping', pong);
ee.once('ping', pong);
ee.removeListener('ping', pong);

ee.emit('ping');
ee.emit('ping');
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

##### 参数

| 范围       | 类型                      |
| ---------- | ------------------------- |
| `event`    | `"will-finish-launching"` |
| `listener` | () => `void`              |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1152

#### removeListener(event, listener)

```ts
removeListener(event: "will-quit", listener: (event: Event) => void): this
```

Removes the specified `listener` from the listener array for the event named`eventName`.

```js
const callback = (stream) => {
  console.log('someone connected!');
};
server.on('connection', callback);
// ...
server.removeListener('connection', callback);
```

`removeListener()` will remove, at most, one instance of a listener from the
listener array. If any single listener has been added multiple times to the
listener array for the specified `eventName`, then `removeListener()` must be
called multiple times to remove each instance.

Once an event is emitted, all listeners attached to it at the
time of emitting are called in order. This implies that any`removeListener()` or `removeAllListeners()` calls _after_ emitting and _before_ the last listener finishes execution
will not remove them from`emit()` in progress. Subsequent events behave as expected.

```js
const myEmitter = new MyEmitter();

const callbackA = () => {
  console.log('A');
  myEmitter.removeListener('event', callbackB);
};

const callbackB = () => {
  console.log('B');
};

myEmitter.on('event', callbackA);

myEmitter.on('event', callbackB);

// callbackA removes listener callbackB but it will still be called.
// Internal listener array at time of emit [callbackA, callbackB]
myEmitter.emit('event');
// Prints:
//   A
//   B

// callbackB is now removed.
// Internal listener array [callbackA]
myEmitter.emit('event');
// Prints:
//   A
```

Because listeners are managed using an internal array, calling this will
change the position indices of any listener registered _after_ the listener
being removed. This will not impact the order in which listeners are called,
but it means that any copies of the listener array as returned by
the `emitter.listeners()` method will need to be recreated.

When a single function has been added as a handler multiple times for a single
event (as in the example below), `removeListener()` will remove the most
recently added instance. In the example the `once('ping')`listener is removed:

```js
const ee = new EventEmitter();

function pong() {
  console.log('pong');
}

ee.on('ping', pong);
ee.once('ping', pong);
ee.removeListener('ping', pong);

ee.emit('ping');
ee.emit('ping');
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

##### 参数

| 范围       | 类型                         |
| ---------- | ---------------------------- |
| `event`    | `"will-quit"`                |
| `listener` | (`event`: `Event`) => `void` |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1168

#### removeListener(event, listener)

```ts
removeListener(event: "window-all-closed", listener: () => void): this
```

Removes the specified `listener` from the listener array for the event named`eventName`.

```js
const callback = (stream) => {
  console.log('someone connected!');
};
server.on('connection', callback);
// ...
server.removeListener('connection', callback);
```

`removeListener()` will remove, at most, one instance of a listener from the
listener array. If any single listener has been added multiple times to the
listener array for the specified `eventName`, then `removeListener()` must be
called multiple times to remove each instance.

Once an event is emitted, all listeners attached to it at the
time of emitting are called in order. This implies that any`removeListener()` or `removeAllListeners()` calls _after_ emitting and _before_ the last listener finishes execution
will not remove them from`emit()` in progress. Subsequent events behave as expected.

```js
const myEmitter = new MyEmitter();

const callbackA = () => {
  console.log('A');
  myEmitter.removeListener('event', callbackB);
};

const callbackB = () => {
  console.log('B');
};

myEmitter.on('event', callbackA);

myEmitter.on('event', callbackB);

// callbackA removes listener callbackB but it will still be called.
// Internal listener array at time of emit [callbackA, callbackB]
myEmitter.emit('event');
// Prints:
//   A
//   B

// callbackB is now removed.
// Internal listener array [callbackA]
myEmitter.emit('event');
// Prints:
//   A
```

Because listeners are managed using an internal array, calling this will
change the position indices of any listener registered _after_ the listener
being removed. This will not impact the order in which listeners are called,
but it means that any copies of the listener array as returned by
the `emitter.listeners()` method will need to be recreated.

When a single function has been added as a handler multiple times for a single
event (as in the example below), `removeListener()` will remove the most
recently added instance. In the example the `once('ping')`listener is removed:

```js
const ee = new EventEmitter();

function pong() {
  console.log('pong');
}

ee.on('ping', pong);
ee.once('ping', pong);
ee.removeListener('ping', pong);

ee.emit('ping');
ee.emit('ping');
```

Returns a reference to the `EventEmitter`, so that calls can be chained.

##### 参数

| 范围       | 类型                  |
| ---------- | --------------------- |
| `event`    | `"window-all-closed"` |
| `listener` | () => `void`          |

##### 返回

`this`

##### 添加于

v0.1.26

##### 重写了

`EventEmitter.removeListener`

##### 定义于

lynxtron/apis/api/app.d.ts:1183

---

### requestSingleInstanceLock()

```ts
requestSingleInstanceLock(additionalData?: Record<any, any>): boolean
```

The return value of this method indicates whether or not this instance of your
application successfully obtained the lock. If it failed to obtain the lock,
you can assume that another instance of your application is already running with
the lock and exit immediately.

I.e. This method returns `true` if your process is the primary instance of your
application and your app should continue loading. It returns `false` if your
process should immediately quit as it has sent its parameters to another
instance that has already acquired the lock.

On macOS, the system enforces single instance automatically when users try to
open a second instance of your app in Finder, and the `open-file` and `open-url`
events will be emitted for that. However when users start your app in command
line, the system's single instance mechanism will be bypassed, and you have to
use this method to ensure single instance.

An example of activating the window of primary instance when a second instance
starts:

#### 参数

| 范围              | 类型                     |
| ----------------- | ------------------------ |
| `additionalData`? | `Record`\<`any`, `any`\> |

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/app.d.ts:1521

---

### setAboutPanelOptions()

```ts
setAboutPanelOptions(options: AboutPanelOptionsOptions): void
```

Set the about panel options. This will override the values defined in the app's
`.plist` file on macOS. See the Apple docs for more details. On Linux, values
must be set in order to be shown; there are no defaults.

If you do not set `credits` but still wish to surface them in your app, AppKit
will look for a file named "Credits.html", "Credits.rtf", and "Credits.rtfd", in
that order, in the bundle returned by the NSBundle class method main. The first
file found is used, and if none is found, the info area is left blank. See Apple
documentation for more information.

#### 参数

| 范围      | 类型                                                                               |
| --------- | ---------------------------------------------------------------------------------- |
| `options` | [`AboutPanelOptionsOptions`](/lynxtron/api/Interface.AboutPanelOptionsOptions.mdx) |

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1533

---

### setActivationPolicy()

```ts
setActivationPolicy(policy: "regular" | "accessory" | "prohibited"): void
```

Sets the activation policy for a given app.

Activation policy types:

- 'regular' - The application is an ordinary app that appears in the Dock and
  may have a user interface.
- 'accessory' - The application doesn’t appear in the Dock and doesn’t have a
  menu bar, but it may be activated programmatically or by clicking on one of its
  windows.
- 'prohibited' - The application doesn’t appear in the Dock and may not create
  windows or be activated.

#### 参数

| 范围     | 类型                                           |
| -------- | ---------------------------------------------- |
| `policy` | `"regular"` \| `"accessory"` \| `"prohibited"` |

#### 返回

`void`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1549

---

### setAppLogsPath()

```ts
setAppLogsPath(path?: string): void
```

Sets or creates a directory your app's logs which can then be manipulated with
`app.getPath()` or `app.setPath(pathName, newPath)`.

Calling `app.setAppLogsPath()` without a `path` parameter will result in this
directory being set to `~/Library/Logs/YourAppName` on _macOS_, and inside the
`userData` directory on _Linux_ and _Windows_.

#### 参数

| 范围    | 类型     |
| ------- | -------- |
| `path`? | `string` |

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1558

---

### setAppUserModelId()

```ts
setAppUserModelId(id: string): void
```

Changes the Application User Model ID to `id`.

#### 参数

| 范围 | 类型     |
| ---- | -------- |
| `id` | `string` |

#### 返回

`void`

#### Platform

win32

#### 定义于

lynxtron/apis/api/app.d.ts:1564

---

### setAsDefaultProtocolClient()

```ts
setAsDefaultProtocolClient(
   protocol: string,
   path?: string,
   args?: string[]): boolean
```

Whether the call succeeded.

Sets the current executable as the default handler for a protocol (aka URI
scheme). It allows you to integrate your app deeper into the operating system.
Once registered, all links with `your-protocol://` will be opened with the
current executable. The whole link, including protocol, will be passed to your
application as a parameter.

> [!NOTE] On macOS, you can only register protocols that have been added to your
> app's `info.plist`, which cannot be modified at runtime. However, you can change
> the file during build time via Electron Forge, Electron Packager, or by editing
> `info.plist` with a text editor. Please refer to Apple's documentation for
> details.

> [!NOTE] In a Windows Store environment (when packaged as an `appx`) this API
> will return `true` for all calls but the registry key it sets won't be
> accessible by other applications. In order to register your Windows Store
> application as a default protocol handler you must declare the protocol in your
> manifest.

The API uses the Windows Registry and `LSSetDefaultHandlerForURLScheme`
internally.

#### 参数

| 范围       | 类型       |
| ---------- | ---------- |
| `protocol` | `string`   |
| `path`?    | `string`   |
| `args`?    | `string`[] |

#### 返回

`boolean`

#### 定义于

lynxtron/apis/api/app.d.ts:1589

---

### setJumpList()

```ts
setJumpList(categories: null | JumpListCategory[]):
  | "ok"
  | "error"
  | "invalidSeparatorError"
  | "fileTypeRegistrationError"
  | "customCategoryAccessDeniedError"
```

Sets or removes a custom Jump List for the application, and returns one of the
following strings:

- `ok` - Nothing went wrong.
- `error` - One or more errors occurred, enable runtime logging to figure out
  the likely cause.
- `invalidSeparatorError` - An attempt was made to add a separator to a custom
  category in the Jump List. Separators are only allowed in the standard `Tasks`
  category.
- `fileTypeRegistrationError` - An attempt was made to add a file link to the
  Jump List for a file type the app isn't registered to handle.
- `customCategoryAccessDeniedError` - Custom categories can't be added to the
  Jump List due to user privacy or group policy settings.

If `categories` is `null` the previously set custom Jump List (if any) will be
replaced by the standard Jump List for the app (managed by Windows).

[!NOTE] If a `JumpListCategory` object has neither the `type` nor the `name`
property set then its `type` is assumed to be `tasks`. If the `name` property is
set but the `type` property is omitted then the `type` is assumed to be
`custom`.

[!NOTE] Users can remove items from custom categories, and Windows will not
allow a removed item to be added back into a custom category until **after** the
next successful call to `app.setJumpList(categories)`. Any attempt to re-add a
removed item to a custom category earlier than that will result in the entire
custom category being omitted from the Jump List. The list of removed items can
be obtained using `app.getJumpListSettings()`.

[!NOTE] The maximum length of a Jump List item's `description` property is 260
characters. Beyond this limit, the item will not be added to the Jump List, nor
will it be displayed.

Here's a very simple example of creating a custom Jump List:

#### 参数

| 范围         | 类型                                                                           |
| ------------ | ------------------------------------------------------------------------------ |
| `categories` | `null` \| [`JumpListCategory`](/lynxtron/api/Interface.JumpListCategory.mdx)[] |

#### 返回

\| `"ok"`
\| `"error"`
\| `"invalidSeparatorError"`
\| `"fileTypeRegistrationError"`
\| `"customCategoryAccessDeniedError"`

#### Platform

win32

#### 定义于

lynxtron/apis/api/app.d.ts:1632

---

### setLoginItemSettings()

```ts
setLoginItemSettings(settings: Settings): void
```

Set the app's login item settings.

To work with Electron's `autoUpdater` on Windows, which uses Squirrel, you'll
want to set the launch path to your executable's name but a directory up, which
is a stub application automatically generated by Squirrel which will
automatically launch the latest version.

For more information about setting different services as login items on macOS 13
and up, see `SMAppService`.

#### 参数

| 范围       | 类型                                               |
| ---------- | -------------------------------------------------- |
| `settings` | [`Settings`](/lynxtron/api/Interface.Settings.mdx) |

#### 返回

`void`

#### Platform

darwin,win32

#### 定义于

lynxtron/apis/api/app.d.ts:1653

---

### setMaxListeners()

```ts
setMaxListeners(n: number): this
```

By default `EventEmitter`s will print a warning if more than `10` listeners are
added for a particular event. This is a useful default that helps finding
memory leaks. The `emitter.setMaxListeners()` method allows the limit to be
modified for this specific `EventEmitter` instance. The value can be set to`Infinity` (or `0`) to indicate an unlimited number of listeners.

Returns a reference to the `EventEmitter`, so that calls can be chained.

#### 参数

| 范围 | 类型     |
| ---- | -------- |
| `n`  | `number` |

#### 返回

`this`

#### 添加于

v0.3.5

#### 继承自

`EventEmitter.setMaxListeners`

#### 定义于

.pnpm/@types+node@18.19.68/node_modules/@types/node/events.d.ts:657

---

### setName()

```ts
setName(name: string): void
```

Overrides the current application's name.

> [!NOTE] This function overrides the name used internally by Electron; it does
> not affect the name that the OS uses.

#### 参数

| 范围   | 类型     |
| ------ | -------- |
| `name` | `string` |

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1660

---

### setPath()

```ts
setPath(name: string, path: string): void
```

Overrides the `path` to a special directory or file associated with `name`. If
the path specifies a directory that does not exist, an `Error` is thrown. In
that case, the directory should be created with `fs.mkdirSync` or similar.

You can only override paths of a `name` defined in `app.getPath`.

By default, web pages' cookies and caches will be stored under the `sessionData`
directory. If you want to change this location, you have to override the
`sessionData` path before the `ready` event of the `app` module is emitted.

#### 参数

| 范围   | 类型     |
| ------ | -------- |
| `name` | `string` |
| `path` | `string` |

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1672

---

### setUserActivity()

```ts
setUserActivity(
   type: string,
   userInfo: any,
   webpageURL?: string): void
```

Creates an `NSUserActivity` and sets it as the current activity. The activity is
eligible for Handoff to another device afterward.

#### 参数

| 范围          | 类型     |
| ------------- | -------- |
| `type`        | `string` |
| `userInfo`    | `any`    |
| `webpageURL`? | `string` |

#### 返回

`void`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1679

---

### setUserTasks()

```ts
setUserTasks(tasks: Task[]): boolean
```

Adds `tasks` to the Tasks category of the Jump List on Windows.

`tasks` is an array of `Task` objects.

Whether the call succeeded.

> [!NOTE] If you'd like to customize the Jump List even more use
> `app.setJumpList(categories)` instead.

#### 参数

| 范围    | 类型                                         |
| ------- | -------------------------------------------- |
| `tasks` | [`Task`](/lynxtron/api/Interface.Task.mdx)[] |

#### 返回

`boolean`

#### Platform

win32

#### 定义于

lynxtron/apis/api/app.d.ts:1692

---

### show()

```ts
show(): void
```

Shows application windows after they were hidden. Does not automatically focus
them.

#### 返回

`void`

#### Platform

darwin

#### 定义于

lynxtron/apis/api/app.d.ts:1699

---

### showAboutPanel()

```ts
showAboutPanel(): void
```

Show the app's about panel options. These options can be overridden with
`app.setAboutPanelOptions(options)`. This function runs asynchronously.

#### 返回

`void`

#### 定义于

lynxtron/apis/api/app.d.ts:1704

---

### startAccessingSecurityScopedResource()

```ts
startAccessingSecurityScopedResource(bookmarkData: string): Function
```

This function **must** be called once you have finished accessing the security
scoped file. If you do not remember to stop accessing the bookmark, kernel
resources will be leaked and your app will lose its ability to reach outside the
sandbox completely, until your app is restarted.

Start accessing a security scoped resource. With this method Electron
applications that are packaged for the Mac App Store may reach outside their
sandbox to access files chosen by the user. See Apple's documentation for a
description of how this system works.

#### 参数

| 范围           | 类型     |
| -------------- | -------- |
| `bookmarkData` | `string` |

#### 返回

`Function`

#### Platform

mas

#### 定义于

lynxtron/apis/api/app.d.ts:1718

---

### whenReady()

```ts
whenReady(): Promise<void>
```

fulfilled when Electron is initialized. May be used as a convenient alternative
to checking `app.isReady()` and subscribing to the `ready` event if the app is
not ready yet.

#### 返回

`Promise`\<`void`\>

#### 定义于

lynxtron/apis/api/app.d.ts:1724
