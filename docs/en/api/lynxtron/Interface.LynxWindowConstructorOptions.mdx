{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}
import \* as Lynx from '@lynx';

[lynxtron](/api/lynxtron/index.mdx) / LynxWindowConstructorOptions

# Interface: LynxWindowConstructorOptions

## Properties

### acceptFirstMouse?

```ts
optional acceptFirstMouse: boolean;
```

Whether clicking an inactive window will also click through to the web contents.
Default is `false` on macOS. This option is not configurable on other platforms.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:146

---

### alwaysOnTop?

```ts
optional alwaysOnTop: boolean;
```

Whether the window should always stay on top of other windows. Default is
`false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:88

---

### autoHideMenuBar?

```ts
optional autoHideMenuBar: boolean;
```

Auto hide the menu bar unless the `Alt` key is pressed. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:154

---

### backgroundColor?

```ts
optional backgroundColor: string;
```

Window's background color as a hexadecimal value, like `#66CD00` or `#FFF` or
`#80FFFFFF` (alpha in #AARRGGBB format is supported if `transparent` is set to
`true`). Default is `#FFF` (white).

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:165

---

### center?

```ts
optional center: boolean;
```

Show window in the center of the screen.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:38

---

### closable?

```ts
optional closable: boolean;
```

Whether window is closable. This is not implemented on Linux. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:76

---

### darkTheme?

```ts
optional darkTheme: boolean;
```

Forces using dark theme for the window, only works on some GTK+3 desktop
environments. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:179

---

### disableAutoHideCursor?

```ts
optional disableAutoHideCursor: boolean;
```

Whether to hide cursor when typing. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:150

---

### enableLargerThanScreen?

```ts
optional enableLargerThanScreen: boolean;
```

Enable the window to be resized larger than screen. Only relevant for macOS, as
other OSes allow larger-than-screen windows by default. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:159

---

### focusable?

```ts
optional focusable: boolean;
```

Whether the window can be focused. Default is `true`. On Windows setting
`focusable: false` also implies setting `skipTaskbar: true`. On Linux setting
`focusable: false` makes the window stop interacting with wm, so the window will
always stay on top in all workspaces.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:83

---

### followParent?

```ts
optional followParent: boolean;
```

Whether to follow parent window if `parent` is set.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:268

---

### frame?

```ts
optional frame: boolean;
```

Specify `false` to create a [Frameless Window]. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:132

---

### fullscreen?

```ts
optional fullscreen: boolean;
```

Whether the window should show in fullscreen. When explicitly set to `false` the
fullscreen button will be hidden or disabled on macOS. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:93

---

### fullscreenable?

```ts
optional fullscreenable: boolean;
```

Whether the window can be put into fullscreen mode. On macOS, also whether the
maximize/zoom button should toggle full screen mode or maximize window. Default
is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:99

---

### ~~fullscreenWindowTitle?~~

```ts
optional fullscreenWindowTitle: boolean;
```

Shows the title in the title bar in full screen mode on macOS for `hiddenInset`
titleBarStyle. Default is `false`.

#### Deprecated

Use `titleBarStyle` instead.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:215

---

### hasShadow?

```ts
optional hasShadow: boolean;
```

Whether window should have a shadow. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:169

---

### height?

```ts
optional height: number;
```

Window's height in pixels. Default is `600`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:18

---

### icon?

```ts
optional icon: string | NativeImage;
```

The window icon. On Windows it is recommended to use `ICO` icons to get best
visual effects, you can also leave it undefined so the executable's icon will be
used.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:117

---

### kiosk?

```ts
optional kiosk: boolean;
```

Whether the window is in kiosk mode. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:111

---

### maxHeight?

```ts
optional maxHeight: number;
```

Window's maximum height. Default is no limit.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:54

---

### maximizable?

```ts
optional maximizable: boolean;
```

Whether window is maximizable. This is not implemented on Linux. Default is
`true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:72

---

### maxWidth?

```ts
optional maxWidth: number;
```

Window's maximum width. Default is no limit.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:50

---

### minHeight?

```ts
optional minHeight: number;
```

Window's minimum height. Default is `0`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:46

---

### minimizable?

```ts
optional minimizable: boolean;
```

Whether window is minimizable. This is not implemented on Linux. Default is
`true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:67

---

### minWidth?

```ts
optional minWidth: number;
```

Window's minimum width. Default is `0`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:42

---

### modal?

```ts
optional modal: boolean;
```

Whether this is a modal window. This only works when the window is a child
window. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:141

---

### movable?

```ts
optional movable: boolean;
```

Whether window is movable. This is not implemented on Linux. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:62

---

### opacity?

```ts
optional opacity: number;
```

Set the initial opacity of the window, between 0.0 (fully transparent) and 1.0
(fully opaque). This is only implemented on Windows and macOS.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:174

---

### paintWhenInitiallyHidden?

```ts
optional paintWhenInitiallyHidden: boolean;
```

Whether the renderer should be active when `show` is `false` and it has just
been created. In order for `document.visibilityState` to work correctly on
first load with `show: false` you should set this to `false`. Setting this to
`false` will cause the `ready-to-show` event to not fire. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:128

---

### parent?

```ts
optional parent: LynxWindow;
```

Specify parent window. Default is `null`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:136

---

### resizable?

```ts
optional resizable: boolean;
```

Whether window is resizable. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:58

---

### roundedCorners?

```ts
optional roundedCorners: boolean;
```

Whether frameless window should have rounded corners on macOS. Default is
`true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:208

---

### show?

```ts
optional show: boolean;
```

Whether window should be shown when created. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:121

---

### simpleFullscreen?

```ts
optional simpleFullscreen: boolean;
```

Use pre-Lion fullscreen on macOS. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:103

---

### skipTaskbar?

```ts
optional skipTaskbar: boolean;
```

Whether to show the window in taskbar. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:107

---

### tabbingIdentifier?

```ts
optional tabbingIdentifier: string;
```

Tab group name, allows opening the window as a native tab on macOS 10.12+.
Windows with the same tabbing identifier will be grouped together. This also
adds a native new tab button to your window's tab bar and allows your `app` and
window to receive the `new-window-for-tab` event.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:264

---

### thickFrame?

```ts
optional thickFrame: boolean;
```

Use `WS_THICKFRAME` style for frameless windows on Windows, which adds standard
window frame. Setting it to `false` will remove window shadow and window
animations. Default is `true`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:221

---

### title?

```ts
optional title: string;
```

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:270

---

### titleBarStyle?

```ts
optional titleBarStyle: "default" | "hidden" | "hiddenInset" | "customButtonsOnHover";
```

The style of window title bar. Default is `default`. Possible values are:

#### Platform

darwin,win32

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:199

---

### trafficLightPosition?

```ts
optional trafficLightPosition: Point;
```

Set a custom position for the traffic light buttons in frameless windows.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:203

---

### transparent?

```ts
optional transparent: boolean;
```

Makes the window [transparent]. Default is `false`. On Windows, does not work
unless the window is frameless.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:184

---

### type?

```ts
optional type: string;
```

The type of window, default is normal window. See more about this below.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:188

---

### useContentSize?

```ts
optional useContentSize: boolean;
```

The `width` and `height` would be used as web page's size, which means the
actual window's size will include window frame's size and be slightly larger.
Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:34

---

### vibrancy?

```ts
optional vibrancy:
  | "appearance-based"
  | "light"
  | "dark"
  | "titlebar"
  | "selection"
  | "menu"
  | "popover"
  | "sidebar"
  | "medium-light"
  | "ultra-dark"
  | "header"
  | "sheet"
  | "window"
  | "hud"
  | "fullscreen-ui"
  | "tooltip"
  | "content"
  | "under-window"
  | "under-page";
```

Add a type of vibrancy effect to the window, only on macOS. Can be
`appearance-based`, `light`, `dark`, `titlebar`, `selection`, `menu`, `popover`,
`sidebar`, `medium-light`, `ultra-dark`, `header`, `sheet`, `window`, `hud`,
`fullscreen-ui`, `tooltip`, `content`, `under-window`, or `under-page`. Please
note that `appearance-based`, `light`, `dark`, `medium-light`, and `ultra-dark`
are deprecated and have been removed in macOS Catalina (10.15).

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:230

---

### visualEffectState?

```ts
optional visualEffectState: "followWindow" | "active" | "inactive";
```

Specify how the material appearance should reflect window activity state on
macOS. Must be used with the `vibrancy` property. Possible values are:

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:193

---

### width?

```ts
optional width: number;
```

Window's width in pixels. Default is `800`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:14

---

### x?

```ts
optional x: number;
```

(**required** if y is used) Window's left offset from screen. Default is to
center the window.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:23

---

### y?

```ts
optional y: number;
```

(**required** if x is used) Window's top offset from screen. Default is to
center the window.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:28

---

### zoomToPageWidth?

```ts
optional zoomToPageWidth: boolean;
```

Controls the behavior on macOS when option-clicking the green stoplight button
on the toolbar or by clicking the Window > Zoom menu item. If `true`, the window
will grow to the preferred width of the web page when zoomed, `false` will cause
it to zoom to the width of the screen. This will also affect the behavior when
calling `maximize()` directly. Default is `false`.

#### Defined in

lynxtron/apis/api/lynx-window.d.ts:257
