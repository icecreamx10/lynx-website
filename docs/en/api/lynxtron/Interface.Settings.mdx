{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}
import \* as Lynx from '@lynx';

[lynxtron](/api/lynxtron/index.mdx) / Settings

# Interface: Settings

## Properties

### args?

```ts
optional args: string[];
```

The command-line arguments to pass to the executable. Defaults to an empty
array. Take care to wrap paths in quotes.

#### Platform

win32

#### Defined in

lynxtron/apis/api/app.d.ts:264

---

### enabled?

```ts
optional enabled: boolean;
```

`true` will change the startup approved registry key and `enable / disable` the
App in Task Manager and Windows Settings. Defaults to `true`.

#### Platform

win32

#### Defined in

lynxtron/apis/api/app.d.ts:271

---

### name?

```ts
optional name: string;
```

value name to write into registry. Defaults to the app's AppUserModelId().

#### Platform

win32

#### Defined in

lynxtron/apis/api/app.d.ts:277

---

### ~~openAsHidden?~~

```ts
optional openAsHidden: boolean;
```

`true` to open the app as hidden. Defaults to `false`. The user can edit this
setting from the System Preferences so
`app.getLoginItemSettings().wasOpenedAsHidden` should be checked when the app is
opened to know the current value. This setting is not available on MAS builds or
on macOS 13 and up.

#### Deprecated

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:233

---

### openAtLogin?

```ts
optional openAtLogin: boolean;
```

`true` to open the app at login, `false` to remove the app as a login item.
Defaults to `false`.

#### Defined in

lynxtron/apis/api/app.d.ts:222

---

### path?

```ts
optional path: string;
```

The executable to launch at login. Defaults to `process.execPath`.

#### Platform

win32

#### Defined in

lynxtron/apis/api/app.d.ts:257

---

### serviceName?

```ts
optional serviceName: string;
```

The name of the service. Required if `type` is non-default. Only available on
macOS 13 and up.

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:251

---

### type?

```ts
optional type: "mainAppService" | "agentService" | "daemonService" | "loginItemService";
```

The type of service to add as a login item. Defaults to `mainAppService`. Only
available on macOS 13 and up.

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:240
