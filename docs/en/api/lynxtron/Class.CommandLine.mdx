{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}
import \* as Lynx from '@lynx';

[lynxtron](/api/lynxtron/index.mdx) / CommandLine

# Class: CommandLine

## Constructors

### new CommandLine()

```ts
new CommandLine(): CommandLine
```

#### Returns

[`CommandLine`](/api/lynxtron/Class.CommandLine.mdx)

## Methods

### appendArgument()

```ts
appendArgument(value: string): void
```

Append an argument to Chromium's command line. The argument will be quoted
correctly. Switches will precede arguments regardless of appending order.

If you're appending an argument like `--switch=value`, consider using
`appendSwitch('switch', 'value')` instead.

[!NOTE] This will not affect `process.argv`. The intended usage of this
function is to control Chromium's behavior.

#### Parameters

| Parameter | Type     |
| --------- | -------- |
| `value`   | `string` |

#### Returns

`void`

#### Defined in

lynxtron/apis/api/command-line.d.ts:14

---

### appendSwitch()

```ts
appendSwitch(the_switch: string, value?: string): void
```

Append a switch (with optional `value`) to Chromium's command line.

[!NOTE] This will not affect `process.argv`. The intended usage of this
function is to control Chromium's behavior.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |
| `value`?     | `string` |

#### Returns

`void`

#### Defined in

lynxtron/apis/api/command-line.d.ts:21

---

### getSwitchValue()

```ts
getSwitchValue(the_switch: string): string
```

The command-line switch value.

This function is meant to obtain Chromium command line switches. It is not meant
to be used for application-specific command line arguments. For the latter,
please use `process.argv`.

> [!NOTE] When the switch is not present or has no value, it returns empty
> string.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |

#### Returns

`string`

#### Defined in

lynxtron/apis/api/command-line.d.ts:32

---

### hasSwitch()

```ts
hasSwitch(the_switch: string): boolean
```

Whether the command-line switch is present.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |

#### Returns

`boolean`

#### Defined in

lynxtron/apis/api/command-line.d.ts:36

---

### removeSwitch()

```ts
removeSwitch(the_switch: string): void
```

Removes the specified switch from Chromium's command line.

> [!NOTE] This will not affect `process.argv`. The intended usage of this
> function is to control Chromium's behavior.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |

#### Returns

`void`

#### Defined in

lynxtron/apis/api/command-line.d.ts:43
