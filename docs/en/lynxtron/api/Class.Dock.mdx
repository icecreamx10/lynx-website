{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

[lynxtron](/lynxtron/api/index.mdx) / Dock

# Class: Dock

## Constructors

### new Dock()

```ts
new Dock(): Dock
```

#### Returns

[`Dock`](/lynxtron/api/Class.Dock.mdx)

## Methods

### bounce()

```ts
bounce(type?: "critical" | "informational"): number
```

an ID representing the request.

When `critical` is passed, the dock icon will bounce until either the
application becomes active or the request is canceled.

When `informational` is passed, the dock icon will bounce for one second.
However, the request remains active until either the application becomes active
or the request is canceled.

> [!NOTE] This method can only be used while the app is not focused; when the
> app is focused it will return -1.

#### Parameters

| Parameter | Type                              |
| --------- | --------------------------------- |
| `type`?   | `"critical"` \| `"informational"` |

#### Returns

`number`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:22

---

### cancelBounce()

```ts
cancelBounce(id: number): void
```

Cancel the bounce of `id`.

#### Parameters

| Parameter | Type     |
| --------- | -------- |
| `id`      | `number` |

#### Returns

`void`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:28

---

### downloadFinished()

```ts
downloadFinished(filePath: string): void
```

Bounces the Downloads stack if the filePath is inside the Downloads folder.

#### Parameters

| Parameter  | Type     |
| ---------- | -------- |
| `filePath` | `string` |

#### Returns

`void`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:34

---

### getBadge()

```ts
getBadge(): string
```

The badge string of the dock.

#### Returns

`string`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:40

---

### getMenu()

```ts
getMenu(): null | Menu
```

The application's dock menu.

#### Returns

`null` \| [`Menu`](/lynxtron/api/Class.Menu.mdx)

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:46

---

### hide()

```ts
hide(): void
```

Hides the dock icon.

#### Returns

`void`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:52

---

### isVisible()

```ts
isVisible(): boolean
```

Whether the dock icon is visible.

#### Returns

`boolean`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:58

---

### setBadge()

```ts
setBadge(text: string): void
```

Sets the string to be displayed in the dock’s badging area.

> [!IMPORTANT] You need to ensure that your application has the permission to
> display notifications for this method to work.

#### Parameters

| Parameter | Type     |
| --------- | -------- |
| `text`    | `string` |

#### Returns

`void`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:67

---

### setIcon()

```ts
setIcon(image: string | NativeImage): void
```

Sets the `image` associated with this dock icon.

#### Parameters

| Parameter | Type                                                             |
| --------- | ---------------------------------------------------------------- |
| `image`   | `string` \| [`NativeImage`](/lynxtron/api/Class.NativeImage.mdx) |

#### Returns

`void`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:73

---

### setMenu()

```ts
setMenu(menu: Menu): void
```

Sets the application's dock menu.

#### Parameters

| Parameter | Type                                   |
| --------- | -------------------------------------- |
| `menu`    | [`Menu`](/lynxtron/api/Class.Menu.mdx) |

#### Returns

`void`

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:79

---

### show()

```ts
show(): Promise<void>
```

Resolves when the dock icon is shown.

#### Returns

`Promise`\<`void`\>

#### Platform

darwin

#### Defined in

lynxtron/apis/api/dock.d.ts:85
