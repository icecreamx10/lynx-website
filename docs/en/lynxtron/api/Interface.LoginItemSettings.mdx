{/\*

- This file is generated by @lynx-js/tool-typedoc.
- Do not edit this file directly.
- @generated
  \*/}

{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}
import \* as Lynx from '@lynx';

[lynxtron](/lynxtron/api/index.mdx) / LoginItemSettings

# Interface: LoginItemSettings

## Properties

### executableWillLaunchAtLogin

```ts
executableWillLaunchAtLogin: boolean;
```

`true` if app is set to open at login and its run key is not deactivated. This
differs from `openAtLogin` as it ignores the `args` option, this property will
be true if the given executable would be launched at login with **any**
arguments.

#### Platform

win32

#### Defined in

lynxtron/apis/api/app.d.ts:131

---

### launchItems

```ts
launchItems: LaunchItems[];
```

#### Defined in

lynxtron/apis/api/app.d.ts:132

---

### ~~openAsHidden~~

```ts
openAsHidden: boolean;
```

`true` if the app is set to open as hidden at login. This does not work on macOS
13 and up.

#### Deprecated

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:91

---

### openAtLogin

```ts
openAtLogin: boolean;
```

`true` if the app is set to open at login.

#### Defined in

lynxtron/apis/api/app.d.ts:83

---

### ~~restoreState~~

```ts
restoreState: boolean;
```

`true` if the app was opened as a login item that should restore the state from
the previous session. This indicates that the app should restore the windows
that were open the last time the app was closed. This setting is not available
on MAS builds or on macOS 13 and up.

#### Deprecated

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:116

---

### status

```ts
status: string;
```

can be one of `not-registered`, `enabled`, `requires-approval`, or `not-found`.

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:122

---

### ~~wasOpenedAsHidden~~

```ts
wasOpenedAsHidden: boolean;
```

`true` if the app was opened as a hidden login item. This indicates that the app
should not open any windows at startup. This setting is not available on MAS
builds or on macOS 13 and up.

#### Deprecated

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:106

---

### wasOpenedAtLogin

```ts
wasOpenedAtLogin: boolean;
```

`true` if the app was opened at login automatically.

#### Platform

darwin

#### Defined in

lynxtron/apis/api/app.d.ts:97
